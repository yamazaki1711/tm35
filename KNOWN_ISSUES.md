# KNOWN_ISSUES — ТМ-35, блок «Люди» на «Обзоре» (/dashboard)

Диагностика от 31.08.2026. Задание координатора: проверить логику блока
«Люди» на «Обзоре», не чинить. Ниже — опись находок с пометками
ПРОВЕРЕНО / ПРЕДПОЛОЖЕНИЕ и происхождением каждого утверждения.

**Оговорка по методологии.** В этом проекте нет системы дисциплины «asd»
(git-репозиторий, hash-манифест, файлы `NODE_*.md`) — она обнаружена
только в другом, не связанном проекте (`~/MAC_ASD/docs/KNOWN_ISSUES.md`,
система KI) и, похоже, была спутана с ТМ-35 при постановке задачи. У
ТМ-35 нет `.git` ни локально (`~/Documents/TM-35`), ни на сервере
(`/opt/kat/tm-app`) — версионирование de facto через ручные бэкапы
(`main.py.bak.*`, `backup_2026NNNN-*/` внутри контейнера). Восстановление
состояния для этой диагностики выполнено через: чтение
`~/Documents/TM-35/CLAUDE.md` (источник истины проекта), прямой запрос к
БД `tm35` и **обязательное** извлечение живого кода из контейнера
(`docker exec tm_backend cat /app/main.py`), а не с диска `/opt/kat/tm-app`
— копия на диске хоста устарела (последняя правка 27.08, в контейнере
уже правки от 30.08), это задокументированный gotcha проекта. Первый
проход диагностики по ошибке читал файлы с диска хоста — пойман и
переделан на живом коде до того, как выводы попали в этот отчёт.

Файл: `home_v2()` — `main.py:3752`, шаблон `templates/home.html`
(маршрут — редирект `/` → `/dashboard`, `main.py:1405`).

---

## 1. Расположение (файл:строка)

**ПРОВЕРЕНО** — цитаты из живого `/app/main.py` и `/app/templates/home.html`
внутри контейнера `tm_backend` на момент 31.08.2026.

- (а) Запрос, дающий план и факт: `main.py:3768–3779`
  ```python
  last_actual_date = query_one(
      "select max(date) as d from daily_progress where actual_crew is not null"
  )["d"]
  today_totals = None
  if last_actual_date:
      today_totals = query_one(
          LATEST_DP_CTE + """
          select sum(planned_crew) as planned, sum(actual_crew) as actual
          from latest_dp where date=%s
          """,
          (last_actual_date,),
      )
  ```
  `LATEST_DP_CTE` определена в `main.py:382`:
  ```sql
  with latest_dp as (
      select distinct on (date, work_id) *
      from daily_progress
      order by date, work_id, updated_at desc
  )
  ```
  Это full-row «последняя запись побеждает» на весь набор источников
  (`excel_import` / `web_form` / `schedule_derived`) — не по-полю, а по
  строке целиком (см. находку №3 ниже).

- (б) Формула дефицита: `templates/home.html:127`
  ```jinja
  {{ (today_totals.planned or 0) - (today_totals.actual or 0) }}
  ```
  warn-подсветка (`home.html:126`): `{% if (planned or 0) - (actual or 0) > 0 %}warn{% endif %}`
  — то есть подсветка есть только когда дефицит положительный; при
  избытке (факт > план, разность отрицательна) подсветки нет, но число
  и подпись остаются как есть.

- (в) Шаблон, печатающий подпись «Дефицит»: `templates/home.html:128`
  ```jinja
  <div class="kpi-label">Дефицит</div>
  ```
  Заголовок блока — `templates/home.html:117`: `Люди на {{ last_actual_date|dmy }}`.

---

## 2. Формула на реальных данных за 28.08 — ПРОВЕРЕНО

Запрос выполнен напрямую к БД `tm35` через `docker exec tm_backend python3`
(тот же `TM35_DSN`, что использует приложение), точная копия запроса
приложения:

```sql
with latest_dp as (
  select distinct on (date, work_id) *
  from daily_progress
  order by date, work_id, updated_at desc
)
select sum(planned_crew) as planned, sum(actual_crew) as actual
from latest_dp where date='2026-08-28';
```

Результат: `{'planned': 6, 'actual': 9}`.

**Подтверждено: −3 = план − факт = 6 − 9.** Формула отработала штатно на
реальных цифрах приложения (не результат отдельного бага в арифметике) —
проблема не в вычислении разности, а в том, что «6» и «9» сами по себе не
то, что кажется (находки №3 и №5), и в том, что разность без пола (floor)
на 0 подписана «Дефицит» независимо от знака (находка №4).

`last_actual_date` на момент проверки = `2026-08-28` (`select max(date)
from daily_progress where actual_crew is not null` → `2026-08-28`) —
подтверждает, что «Обзор» сейчас действительно показывает именно этот
день, не другой.

---

## 3. Происхождение «Факт = 9» — ПРОВЕРЕНО, ветка (а)

Разобраны сырые строки `daily_progress` за 28.08 (все источники) и
результат `LATEST_DP_CTE` (по каждому `work_id` — строка с максимальным
`updated_at`):

| work_id | шифр | источник-победитель | planned (в победившей строке) | actual (в победившей строке) |
|---|---|---|---|---|
| 109 | TM35-MAIN-009 | web_form (Координатор, 29.08 07:48) | NULL | 0 |
| 113 | TM35-MAIN-013 | web_form | NULL | 0 |
| 125 | TM35-MAIN-025 | web_form | NULL | 0 |
| 127 | TM35-MAIN-027 | web_form | NULL | **5** |
| 141 | TM35-MAIN-041 | web_form | NULL | **2** |
| 147 | TM35-MAIN-047 | web_form | NULL | **2** |
| 165 | TM35-MAIN-065 | schedule_derived | 3 | NULL |
| 167 | TM35-MAIN-067 | web_form | NULL | 0 |
| 190 | TM35-MAIN-090 | schedule_derived | 2 | NULL |
| 196 | TM35-MAIN-096 | web_form | NULL | 0 |
| 217 | TM35-AUX-117 | schedule_derived | 1 | NULL |
| остальные (159,161,162,178,179,260) | — | excel_import, оба поля NULL | — | — |

`sum(actual) = 5 + 2 + 2 + 0×5 = 9`. `sum(planned) = 3 + 2 + 1 = 6`.

**Ветка (а) подтверждена: факт = сумма фактической численности по
нескольким разным работам (127/141/147), а не одна запись.** Все три
вклада (5+2+2) внесены пользователем «Координатор» (`created_by=11`)
29.08 в 07:51–07:52 задним числом за 28.08, через `/api/shift/cell`
(«экран смены», не «/form»). «Смитаненко» (см. ниже) к этим трём записям
отношения не имеет.

Ветка (б) — не подтверждена *для этих трёх строк* (записей 60941/60943/
60945 никогда не существовало версии со значением 6 — они с самого
начала созданы как 5/2/2 и не редактировались повторно). Но применительно
к другой работе (work_id 101) обнаружена смежная, более серьёзная
аномалия — см. находку №5, она отдельно объясняет несовпадение с фактом
координатора и **не входит ни в одну из трёх заявленных веток**.

Ветка (в) — не подтверждена: и «Обзор», и `/shift` читают одно и то же
поле `actual_crew` из одной таблицы `daily_progress`, дата совпадает
(28.08 в обоих местах).

**План = 6 — самостоятельная, более крупная находка**, см. №4.

---

## 4. «Латест-wins» — план теряется вместе с фактом того же дня — ПРОВЕРЕНО

`LATEST_DP_CTE` выбирает **всю строку целиком** по `(date, work_id)` с
максимальным `updated_at` — не мёрджит поля по-отдельности. Источники
пишут разные поля: Excel — только план (`planned_crew`), `/api/shift/cell`
(веб-форма) — только то поле, которое реально редактировали за раз (в
основном `actual_crew`/`fact_pct`), `planned_crew` в веб-форме всегда
`NULL`.

Если по `(work_id, date)` веб-форма отметилась позже, чем импорт Excel
(что здесь так для 8 из 11 работ 28.08 — Excel импортирован в 03:47,
веб-форма — с 07:48 до 07:52 того же дня), то в `latest_dp` побеждает
строка веб-формы **целиком**, и план из Excel-строки для этого дня
пропадает из суммы бесследно (не отображается как «0» с пометкой —
просто не участвует в `sum`).

Проверено запросом:

```sql
select sum(planned_crew) as any_source_plan_sum,
       count(distinct work_id) as works_with_plan
from daily_progress where date='2026-08-28' and planned_crew is not null;
-- {'any_source_plan_sum': 34, 'works_with_plan': 11}
```

vs. то, что реально показывает «Обзор» — **6**. Реальный суммарный план
Excel на 28.08 по всем работам с известной численностью — **34 чел.**, не
6. «План: 6» на «Обзоре» — это план только тех 3 работ, где в тот день
не было ни одной записи с веб-формы (165, 190, 217, все `schedule_derived`);
план остальных 8 работ (28 чел.) не потерян физически в БД, но исчез из
этой конкретной суммы из-за full-row дедупа.

**Это не про смысл «дефицит vs избыток» — это отдельный, более базовый
дефект: и «План», и «Факт» на «Обзоре» уже сейчас не то число, которое
интуитивно ожидает инженер ПТО** («суммарная плановая численность на
объекте сегодня»), а «сумма плана только по работам, которые в этот день
ещё не трогала веб-форма». Совпадение 6 из примера координатора с «планом
только 3 автоплан-работ» — не случайность разового дня, а системное
поведение при любом дне, где Excel и веб-форма затрагивают одни и те же
работы.

---

## 5. Расхождение с «отчётом готовности» (факт=6, запись smetanenko) — ПРОВЕРЕНО частично, дальше ПРЕДПОЛОЖЕНИЕ

Пользователь `smetanenko` (`app_user.id=17`, роль `executor`) реально
существует и реально заходил в систему 29.08 в 10:42–10:48 (`login_log`,
IP `172.18.0.2`, `python-requests` — похоже на API-клиент/скрипт, не
браузер). **Но не по работам 127/141/147** (которые дали «9»), а по
**work_id=101** — другая работа, не входящая в вышеприведённую таблицу
за 28.08 вовсе.

`audit_log` фиксирует 4 успешных вызова `/api/shift/cell` от
`user_id=17` в 10:43:00–10:43:40, все на `date=2026-08-28, work_id=101`:
дважды `field=pct, value=42.0` и дважды `field=crew, value=6` (id записей
audit_log: 243–246), с двумя разными `entity_id` daily_progress —
60950 и 60952.

**Аномалия:** ни `id=60950`, ни `id=60952` не существуют в текущей
таблице `daily_progress` — ни под этими id, ни вообще как строка
`(work_id=101, date=2026-08-28)` под любым источником. Проверено прямым
запросом `select * from daily_progress where id in (60950,60952) or
(work_id=101 and date='2026-08-28')` → 0 строк. При этом:

- В самом коде `/api/shift/cell` (`main.py:2760–2807`) `INSERT ...
  ON CONFLICT (date, work_id, source) DO UPDATE ... RETURNING id`, затем
  `INSERT INTO audit_log (...)` — обе вставки в одной транзакции
  (`run_in_transaction`, единственный `commit()` в конце). Если бы
  транзакция откатилась, `audit_log` тоже не сохранился бы — но он
  сохранён и виден сейчас. Значит, на момент коммита строка
  `daily_progress` действительно существовала.
- Ни в `main.py`, ни в `analytics.py`, ни в `weather_sync.py`, ни в
  `tm_patch.py`, ни в `db.py` нет ни одного `DELETE FROM daily_progress`
  — прямого следа, кто/что могло убрать эту строку впоследствии, в коде
  не найдено. Cron/планировщика в контейнере не обнаружено.
- Второй вызов (второй `pct`, 18 секунд спустя) получил **новый** id
  (60952), а не тот же 60950, хотя ключ конфликта `(date, work_id,
  source='web_form')` идентичен первому — это возможно только если
  строка 60950 уже отсутствовала к моменту второго вызова (иначе
  `ON CONFLICT DO UPDATE` вернул бы тот же id).

**Вывод по этой находке: сам факт «Факт=9 вместо 6» на «Обзоре»
объясняется веткой (а) (находка №3) и с записью smetanenko не связан
напрямую — smetanenko писал в другую работу (101), которая на 28.08 в
принципе не участвует в сумме.** Но обнаружена смежная и более тревожная
проблема: **как минимум одна успешно закоммиченная запись
`daily_progress` бесследно пропала из таблицы уже после подтверждённого
коммита**, при этом её «тень» в `audit_log` осталась и создаёт видимость
доступной истории там, где реальных данных больше нет. Корень этого
явления (что физически удалило/перезаписало строку и почему это не
видно ни в одном найденном коде) — **не установлен**, требует отдельного
расследования (возможно, вне `/app`-кода: ручное вмешательство в БД,
другой процесс с доступом к тому же `tm35_app`, либо теперь уже
недоступная более ранняя версия `main.py`, действовавшая в момент записи
29.08 10:43 — код с тех пор менялся минимум один раз, 30.08 добавлена
проверка прав `smr:write`, которой не было на момент инцидента). **Не
чинить** — по заданию, только зафиксировано.

---

## 6. Соседние карточки — «Отставание от графика», «Прошло срока», «Дефицит»

**ПРОВЕРЕНО**, находок кроме уже описанной для «Дефицит» — нет:

- **«Дефицит»** (`home.html:127–128`) — воспроизводит находку в п.2/4:
  сырая разность `план − факт` без `max(0, …)`, подпись не меняется на
  знак. При факте больше плана (избыток) число уходит в минус, но
  подпись всё равно «Дефицит» — семантически неверно читается как «на
  −3 человека не хватает», хотя фактически на площадке было на 3
  человека больше, чем спланировано (или, как показано в п.4, «план»
  здесь занижен из-за дедупа, так что сам смысл ещё и не факт что верный
  ориентир).
- **«Отставание от графика»** (`home.html:96–98`) — это не разность, а
  отношение `crit.elapsed_pct / evm.weighted_pct` (доля прошедшего срока
  делённая на долю сделанного). Обе величины неотрицательны, деление на
  ноль исключено проверкой `evm.weighted_pct` на истинность (0 —
  falsy → `ratio = None` → показывается «—»). Направление знака
  объяснено рядом текстом-подсказкой («1× — по графику, больше —
  медленнее»). Аналогичного бага не найдено.
- **«Прошло срока»** (`home.html:91–94`) — прямой процент
  (`crit.elapsed_pct`), не разность и не отношение двух независимых
  величин, инвертировать нечего. Аналогичного бага не найдено.
- **«Просрочено работ»** (`home.html:159`) — счётчик (`crit.overdue_count`),
  всегда ≥0 по построению (иначе бы не считался «просроченным»).
  Аналогичного бага не найдено.

---

## Вопрос координатору (обязателен, не выбирать молча)

Семантика «дефицит vs избыток» для карточки «Люди» на «Обзоре» — нужно
решение. Три варианта:

1. **Дефицит = max(0, план − факт), избыток — отдельной величиной/знаком**
   (например, вторая карточка «Избыток» появляется только когда факт >
   план, либо число со знаком `+`, зелёного цвета, с той же подписью
   «Дефицит», но явно неотрицательное).
2. **Единый «баланс» со знаком** — одна карточка, нейтральная подпись
   (например, «Баланс» или «План к факту»), число может быть
   отрицательным (перебор) или положительным (нехватка), с явной
   легендой знака.
3. **Что показывать при факт = план (0)** — отдельно от выбора 1/2:
   ноль показывать как «0» или как прочерк «—» (сейчас — как «0», без
   отдельной обработки).

**Отдельно, вне вопроса о знаке**: находка №4 (план теряет вклад
Excel-строк того же дня, если веб-форма трогала ту же работу) искажает
и «План», и «Факт» уже сегодня, независимо от выбора 1/2/3 выше — с этим
предлагается разбираться отдельным заданием, не как часть текущей
семантики знака (по правилу «находка не равна новой задаче» — заносится
как открытый вопрос, фронт работ не разворачивается без решения
координатора).

---

## 7. Проверка гипотезы единого корня (п.4 и п.5) — 31.08.2026, ОПРОВЕРГНУТА

**Гипотеза координатора:** full-row last-write-wins в `LATEST_DP_CTE` по
ключу `(work_id, date)` — это ОДИН механизм, который одновременно (а)
затирает план Excel-строки, когда веб-форма пишет тот же день только с
фактом, и (б) объясняет исчезновение записи smetanenko (work_id=101,
crew=6) — потому что её «победила» в CTE какая-то более поздняя строка.

**Проверено прямым запросом — ОПРОВЕРГНУТА.**

```sql
select id, date, source, planned_crew, actual_crew, created_by, created_at, updated_at
from daily_progress where work_id = 101 order by date, updated_at;
-- 0 rows
```

По `work_id=101` в `daily_progress` **нет ни одной строки — ни за 28.08,
ни за любую другую дату, ни от какого источника**. Работа `TM35-MAIN-001`
в таблице `work` существует и валидна (`status='done_physically'`), т.е.
дело не в удалённой/переномерованной работе.

Механизм п.4 (CTE-дедуп прячет строку) **не может объяснить это**: он
работает через `DISTINCT ON (date, work_id) ORDER BY updated_at DESC` —
проигравшая строка при этом остаётся физически в таблице, просто не
выбирается в `latest_dp`. Если бы гипотеза была верна, строка smetanenko
(id 60950 или 60952) осталась бы в `daily_progress` под тем же id, только
не побеждала бы в CTE. Прямой запрос по `id in (60950,60952)` и по
`work_id=101 and date=2026-08-28` даёт 0 строк при любом источнике —
значит, строка отсутствует **физически**, не просто «спрятана» дедупом.

Дополнительно проверено и исключено:
- **Ограничение конфликта** — `UNIQUE (date, work_id, source)`
  (`daily_progress_date_work_id_source_key`) — ровно то, что использует
  код `/api/shift/cell` в `ON CONFLICT`, несовпадения ключа нет.
- **Триггеры на `daily_progress`** — отсутствуют (`pg_trigger` пуст для
  этой таблицы), т.е. удаления через триггер быть не могло.
- **`DELETE FROM daily_progress`** — не найден ни в одном `.py`-файле
  контейнера (`main.py`, `analytics.py`, `weather_sync.py`, `tm_patch.py`,
  `db.py`), cron/планировщика в контейнере нет.
- **Журнал Postgres** — `log_statement = none` на сервере, поэтому
  проверить по логам БД, кто выполнил физическое удаление, нельзя;
  дальше этого пункта расследование средствами, доступными для
  диагностики (без записи в БД и без правок кода), не продвигается.

**Вывод:** находки №4 и №5 — это **два разных дефекта, не один корень**.
№4 (потеря плана Excel при позднейшей записи веб-формы в тот же день) —
воспроизводимый и понятный побочный эффект `DISTINCT ON` в
`LATEST_DP_CTE`, строка при этом жива в таблице. №5 (пропажа записи
smetanenko) — **более серьёзный, отдельный дефект: подтверждённо
закоммиченная строка физически отсутствует в таблице**, причина
физического удаления/недохождения до диска не установлена доступными
средствами диагностики. Требует либо доступа к WAL/бэкапам БД за
29.08 10:43–11:00, либо признания как нерешённого при нынешнем уровне
логирования — решение за координатором.

---

## 8. Ведомость охвата `LATEST_DP_CTE` — какие карточки «Обзора» его используют

Проверено построчно для каждой карточки на `home.html` (маршрут `/dashboard`,
`main.py:3752`), связанной с людьми/трудоёмкостью/сроками:

| Карточка на «Обзоре» | Источник значения | Читает через `LATEST_DP_CTE`? |
|---|---|---|
| **Люди → План, чел.** | `today_totals.planned`, `main.py:3775` (`home_v2`) | **ДА** |
| **Люди → Факт, чел.** | `today_totals.actual`, `main.py:3775` (`home_v2`) | **ДА** |
| **Люди → Дефицит** | разность двух полей выше, `home.html:127` | **ДА** (косвенно, через оба входа) |
| **Выполнено работ, %** | `evm.weighted_pct`, `get_evm_data()` → `compute_weighted_progress()`, `main.py:636–657` — сумма `planned_crew` **напрямую** из `daily_progress where source='excel_import'` (без CTE) + `work.fact_pct`/`baseline_schedule.plan_crew` | **НЕТ** — не подвержена дефекту №4 |
| **Прошло срока, %** | `crit.elapsed_pct`, `get_criticality_data()` → `compute_schedule_position()`, `main.py:753–757` — чистая арифметика дат (`get_display_window()` + `baseline_schedule`), `daily_progress` не участвует вообще | **НЕТ** |
| **Отставание от графика (×N)** | Отношение `crit.elapsed_pct / evm.weighted_pct`, считается прямо в шаблоне (`home.html:99`), отдельного запроса нет | **НЕТ** (наследует «нет» от обоих входов) |
| **Просрочено работ / Плановый срок / Прогноз окончания** | `crit.overdue_count`/`baseline_date`/`forecast_date`, та же `get_criticality_data()` — из `baseline_schedule`, `daily_progress` не участвует | **НЕТ** |

**Побочно найдено при разборе `get_criticality_data()` (main.py:700–776):**
внутри неё **отдельно, независимо от `home_v2()`**, тем же самым
`LATEST_DP_CTE`-запросом (`main.py:737–740`, буквально идентичная форма
`select sum(planned_crew) as p, sum(actual_crew) as a from latest_dp
where date=%s`) считаются `crit.required_crew`/`crit.actual_crew`/
`crit.deficit`/`crit.coverage_pct` — те же план/факт на тот же день,
дублирующий ту же ошибку п.4, что и в `home_v2()`. Эти поля **сейчас нигде
не выводятся** — ни на `home.html`, ни на `critical.html` (проверено
`grep` по обоим шаблонам, совпадений нет) — то есть это на сегодня мёртвый
код, но два независимых места считают одно и то же и оба несут дефект
№4 — если поля когда-нибудь начнут выводить, дефект придётся чинить
дважды, либо свести к одному вызову.

**Итог охвата:** дефект №4 (потеря плана при full-row дедупе) затрагивает
**только блок «Люди»** на «Обзоре» среди видимых карточек — остальные три
секции («Выполнено работ», «Прошло срока», «Отставание от графика»,
«Сроки») трудоёмкость и план считают из `daily_progress`/`baseline_schedule`
напрямую, без `LATEST_DP_CTE`, и этой болезнью не больны.

---

## 9. Наблюдение по №5, включено 31.08.2026 — не патч, добавление слежения

По заданию координатора: расследование №5 (пропажа записи smetanenko)
дальше средствами разовой диагностики не продвигается — переведено в
режим наблюдения на будущее. **`daily_progress` не менялась, поведение
приложения не менялось** — добавлены только логирование и триггер-ловушка.

### 9.1. Логирование Postgres — включено, точечно на базу `tm35`

`log_statement` — параметр с контекстом `superuser`: `tm35_app` не может
его выставить даже через `ALTER DATABASE`/`ALTER ROLE` (проверено, оба
дают `permission denied to set parameter`). Единственный суперпользователь
на этом Postgres — `kat_admin`, общий для всего кластера (в том же
инстансе — `kat_core`/Левашово). Чтобы не задеть соседний проект (правило
изоляции), применено **точечно**, не `ALTER SYSTEM`:

```sql
ALTER DATABASE tm35 SET log_statement = 'mod';
```

Выполнено от `kat_admin` 31.08.2026. Действует только на новые
подключения к базе `tm35`; `kat_core` и остальные базы кластера не
затронуты (проверено — `pg_db_role_setting` содержит запись только для
`tm35`). Рестарт Postgres не требовался и не выполнялся.

**Путь к логам.** `logging_collector=off` на этом инстансе — файла на
диске нет, вывод идёт в stderr контейнера. Смотреть: `docker logs
kat_postgres` (на сервере `kat-core`, `147.45.251.207`). Пример строки
(тестовый прогон на 31.08.2026 01:21:42 UTC на служебной таблице
`log_probe_012142`, тут же удалённой):

```
2026-08-31 01:21:42.208 UTC [284707] LOG:  statement: update log_probe_012142 set id = id
2026-08-31 01:21:42.210 UTC [284707] LOG:  statement: delete from log_probe_012142 where true
```

Проверено: новое подключение через `TM35_DSN` сразу видит
`current_setting('log_statement') = 'mod'`. **Ограничение:** `'mod'`
логирует INSERT/UPDATE/DELETE/TRUNCATE/DDL без разбора — не только
`daily_progress`; при разборе следующего случая понадобится `grep` по
имени таблицы и/или по времени инцидента, отдельного файла-среза для
одной таблицы это не даёт.

### 9.2. Триггер-ловушка на `daily_progress`

Добавлено (только `AFTER UPDATE OR DELETE`, на `INSERT` не вешалось, как
просил координатор):

```sql
create table daily_progress_audit_trap (
    id bigserial primary key,
    op text not null,               -- 'UPDATE' | 'DELETE'
    dp_id bigint,
    old_row jsonb not null,         -- полный снимок строки ДО операции
    db_role text not null,
    application_name text,
    client_addr inet,
    pid integer not null,
    changed_at timestamptz not null default now()
);

create function daily_progress_audit_trap_fn() returns trigger as $$
begin
    insert into daily_progress_audit_trap
        (op, dp_id, old_row, db_role, application_name, client_addr, pid, changed_at)
    values
        (TG_OP, OLD.id, row_to_json(OLD)::jsonb, current_user,
         current_setting('application_name', true),
         inet_client_addr(), pg_backend_pid(), now());
    return OLD;
end;
$$ language plpgsql;

create trigger daily_progress_audit_trap_trg
after update or delete on daily_progress
for each row execute function daily_progress_audit_trap_fn();
```

Проверено на служебной строке (id 60954, вставлена и тут же убрана самим
тестом, реальных данных не касалось): триггер поймал и `UPDATE`
(`old_row.planned_crew=1`, до изменения на 2), и `DELETE`
(`old_row.planned_crew=2`, состояние прямо перед удалением) — оба со
`db_role=tm35_app`, `client_addr`, `pid`, точным `changed_at`.

**Ограничение, обнаруженное сразу:** `application_name` у приложения
пустой (`''`) — psycopg2-подключения в `db.py` его не выставляют. Триггер
поэтому пока не может отличить «это сделал `/api/shift/cell`» от «это
сделал произвольный скрипт с теми же правами `tm35_app`» иначе как по
`client_addr`/`pid`/времени (сверяя с `audit_log`, если приложение при
этом ещё и обычным путём логирует само действие — как в №5). Если
координатору нужно более точное разделение источника — отдельная,
не выполненная сейчас правка: добавить `options=--application_name=...`
в `TM35_DSN`, либо `SET application_name` в начале каждого запроса
в `db.py`. Не делал — это уже правка кода приложения, а не только
наблюдение.

### 9.3. Сверка масштаба — счёт и перечень, без восстановления

Сопоставлены все `audit_log` со `entity_type='daily_progress'` (151 запись
всего, по типам действия: `gantt_cell_edit` — 77, `web_form_submit` — 57,
`shift_cell_edit` — 17) против фактического содержимого `daily_progress`
на 31.08.2026.

**Всего различных `daily_progress.id`, когда-либо упомянутых в
`audit_log`: 60. Из них физически отсутствуют в таблице сейчас: 8
(13,3%).** Это **не ограничено ни 28.08, ни `/api/shift/cell`** — бьёт по
всем трём путям записи (`/form`, `/gantt`, `/shift`) и тянется минимум с
14.08:

| dp_id | впервые в audit_log | в последний раз | audit-строк | действие(я) | пользователь(и) |
|---|---|---|---|---|---|
| 4430 | 14.08 16:56 | 14.08 16:56 | 1 | web_form_submit | 1 (Веб-форма, общий вход) |
| 46024 | 19.08 05:08 | 19.08 21:25 | 5 | gantt_cell_edit | 1 |
| 50332 | 19.08 21:30 | 19.08 21:30 | 1 | gantt_cell_edit | 1 |
| 50374 | 27.08 00:11 | 27.08 00:17 | 48 | web_form_submit | 1 |
| 50422 | 27.08 02:19 | 27.08 02:19 | 1 | web_form_submit | 1 |
| 60949 | 29.08 08:28 | 29.08 08:28 | 1 | shift_cell_edit | 17 (Сметаненко) |
| 60950 | 29.08 10:43:00 | 29.08 10:43:00 | 2 | shift_cell_edit | 17 |
| 60952 | 29.08 10:43:18 | 29.08 10:43:40 | 2 | shift_cell_edit | 17 |

**Важная оговорка о цифрах.** 48 из 61 audit-строк, указывающих на
отсутствующие id, приходятся на ОДНУ пропавшую строку (50374) с частыми
правками поля за 6 минут — считать «61 audit-строка» как «61 инцидент»
неверно; правильная единица счёта — **8 различных пропавших id**, не
строк audit_log. Также: у smetanenko (`user_id=17`) пропавших строк
**три**, не две, как считалось в п.5 — 60949 (29.08 08:28, до
разбираемого инцидента 10:43) не был замечен на предыдущем проходе,
всплыл только при сплошной сверке.

**Только счёт и перечень — восстановление не выполнялось**, ничего в
`daily_progress` не менялось.

---

## 10. Варианты решения №4 (потеря плана при full-row дедупе) — подготовлено, НЕ применено

Три варианта, от дешёвого к дорогому. Ничего из этого не внедрено — по
заданию координатора, только описание для выбора. Все варианты сохраняют
исходные строки `daily_progress` как есть (правило проекта — не
переписывать историю), меняется только то, как строится `latest_dp`
(единая точка — `LATEST_DP_CTE`, `main.py:382`, используется 22 раза по
файлу, любой вариант меняет её ОДИН раз, а не все 22 места).

### Вариант А — per-field merge через `DISTINCT ON` по каждому полю отдельно (рекомендуется)

Вместо одной строки-победителя — план и факт выбираются независимо, у
каждого своя «последняя ненулевая запись»:

```sql
with plan_dp as (
    select distinct on (date, work_id) date, work_id, planned_crew
    from daily_progress
    where planned_crew is not null
    order by date, work_id, updated_at desc
),
fact_dp as (
    select distinct on (date, work_id) date, work_id, actual_crew, fact_pct, comment, reason_code
    from daily_progress
    where actual_crew is not null or fact_pct is not null
    order by date, work_id, updated_at desc
)
select coalesce(p.date, f.date) as date, coalesce(p.work_id, f.work_id) as work_id,
       p.planned_crew, f.actual_crew, f.fact_pct, f.comment, f.reason_code
from plan_dp p full outer join fact_dp f using (date, work_id)
```

- **Плюс:** правит проблему в одном месте (`LATEST_DP_CTE`), 22 места
  использования получают исправление бесплатно, если продолжают
  обращаться к тем же именам колонок `latest_dp.*`.
- **Минус/риск:** нужно явно решить по каждой колонке, кто её «источник
  правды» (план vs факт) — если появится новая колонка в будущем, её
  тоже придётся отнести к одной из двух групп, иначе она молча выпадет
  из обеих CTE (та же болезнь, что чинится, только для новой колонки).
  Нужно свериться, кто ещё читает `latest_dp.id`/`latest_dp.source`/
  `latest_dp.created_by` — в объединённой по `full outer join` строке
  единого «id одной строки» уже нет, эти поля теряют смысл как есть
  (беглый `grep` по `LATEST_DP_CTE`-местам на этот случай не делался —
  нужен перед внедрением).
- **Стоимость:** низкая-средняя. Один запрос переписать, один раз
  проверить на всех 22 местах, что никто не читает потерявшие смысл
  поля.

### Вариант Б — то же самое через оконные функции (`first_value`)

```sql
select distinct date, work_id,
    first_value(planned_crew) over (partition by date, work_id
        order by (planned_crew is null), updated_at desc) as planned_crew,
    first_value(actual_crew) over (partition by date, work_id
        order by (actual_crew is null), updated_at desc) as actual_crew,
    ...
from daily_progress
```

- Логически эквивалентно варианту А (латест ненулевое значение по
  каждой колонке), один проход по таблице вместо двух CTE.
- **Плюс:** чуть компактнее, план исполнения может быть дешевле (один
  скан вместо двух + full outer join) — не измерялось, таблица небольшая
  (несколько тысяч строк), выигрыш вряд ли заметен на практике.
- **Минус:** тот же риск с «полями без источника правды», что и в А, плюс
  синтаксис с оконными функциями читается сложнее для не-SQL-специалиста,
  который потом будет это поддерживать (ПТО/подрядчик не пишет SQL, но
  разработчик после текущей команды — может).
- **Стоимость:** сопоставима с А.

### Вариант В — разделить план и факт на разные сущности (таблицы/источник)

Завести отдельное понятие «плановая численность на день» отдельно от
«запись о смене» — например, отдельная таблица
`daily_plan(date, work_id, planned_crew, source, updated_at)`, куда
пишут только Excel-импорт и `schedule_derived`; `daily_progress`
остаётся только про факт (то, что уже фактически и означает веб-форма/
экран смены). Тогда полностью нет самого вопроса «чья строка победила»
для плана — он никогда не смешивается с фактом на уровне строки.

- **Плюс:** структурно устраняет причину, а не мёржит её результат —
  дальше подобная путаница по конструкции невозможна.
- **Минус:** это уже не правка запроса, а миграция схемы — новая
  таблица, перенос исторических значений плана из `daily_progress` в
  неё, правка всех 22 мест (они читают `latest_dp`, единой точки замены
  без изменения формы данных здесь уже не получится — придётся менять
  каждый вызов, где нужен план, отдельно от каждого вызова, где нужен
  факт), риск пропустить потребителя. Самый дорогой и самый рискованный
  вариант по объёму правок, хотя самый чистый архитектурно.
- **Стоимость:** высокая. Не рекомендуется как первый шаг — вариант А
  решает ту же проблему на данных, которые есть, без миграции.

**Выбор — за координатором.** Ничего из трёх вариантов не применено.

---

## 11. Расхождение `work.status` / `work.fact_pct` — 31.08.2026, только опись, не чинить

Найдено попутно при разборе фильтра «Есть факт» на `/gantt` (координатор
сам обратил внимание на пример `TM35-MAIN-047`). **Приоритет не
поднимается, не сегодня** — только счёт и перечень, как просил
координатор.

Проверено запросом по всем 169 работам, `DONE_STATUSES =
{"done_physically", "submitted", "accepted", "closed", "cancelled"}`
(`analytics.py:17`) — в реальных данных статус принимает всего 3
значения: `not_started`, `in_progress`, `done_physically`.

**Направление 1 — `status = done_physically`, но `fact_pct < 100`
(отмечена закрытой физически, а процент не дотянут до 100): 1 работа.**

| Шифр | fact_pct |
|---|---|
| TM35-MAIN-001 | 42.0 |

**Направление 2 — `status != done_physically`, но `fact_pct = 100`
(процент показывает полную готовность, а статус не переведён в
закрытый): 2 работы.**

| Шифр | status | fact_pct |
|---|---|---|
| TM35-MAIN-047 | in_progress | 100.0 |
| TM35-MAIN-077 | not_started | 100.0 |

Итого 3 работы из 169 (1,8%) с рассинхроном между двумя полями,
которые в интерфейсе должны бы согласовываться (статус и процент
обычно читаются вместе — например, на `/gantt` при фильтре «только
активные» + «есть факт»). Направление 2 — практическое следствие уже
описано в разделе о фильтре «Есть факт»: `TM35-MAIN-047` проходит оба
фильтра одновременно и выглядит как «идущая работа», хотя по проценту
уже полностью готова.

Не устанавливалось: какое из двух полей в каждом случае верное (реальный
% выполнения или реальный статус) — это вопрос к ПТО, не техническая
проверка. Не чинил, не нормализовал.
