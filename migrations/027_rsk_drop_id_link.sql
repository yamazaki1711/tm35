-- Откат связи ИД ↔ РСК (координатор, 07.09.2026): связь не проектировалась
-- и не согласовывалась — введена самодеятельно в 75cd903 (миграция 024,
-- таблица rsk_processing_id_row) вместе со слоем "Отработка предписаний".
-- Модуль ИД в эксплуатации, чужая функциональность в него не тянется.
--
-- Таблица была пуста в проде на момент отката (тот же вывод, что и для
-- миграции 025 — слой 2 контура РСК ещё не использовался вживую) —
-- дропать безопасно, данных не теряем.

begin;

drop table if exists rsk_processing_id_row;

commit;
