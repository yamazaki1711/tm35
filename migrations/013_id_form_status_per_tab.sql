-- Правка 012: справочник статусов оказался НЕ общим для всех вкладок,
-- вопреки прямому ответу ПТО ("список идентичен на всех проверенных
-- вкладках"). Проверено по факту (28.08.2026, комплексная сверка блоков
-- под таблицей на всех 18 вкладок): 16 из 18 используют один и тот же
-- 8-позиционный список (да/Сделано в эл.виде · нет/Не сделано ·
-- Х/Работы не производятся · Передано на проверку · Подписано · Нет
-- проектного решения · Есть замечания · Нет лаборатории). Только вкладка
-- «ОПН» использует другой, 9-позиционный список с циклом проверки РСК
-- (Первичная проверка РСК · Устранение замечаний 1 · Повторная проверка
-- РСК · Устранение замечаний 2), на который опирается весь регламент
-- просрочки в ТЗ (раздел 5) — то есть этот регламент в текущем виде
-- вычислим только для ОПН, не для остальных 16 вкладок. См. отчёт,
-- находка №1 — требует решения координатора/ПТО, какой список
-- актуален.
--
-- id_form_entry не трогаем — status_id уже ссылается на id_form_status.id
-- (суррогатный ключ), смена уникальности с (code) на (tab_id, code) не
-- меняет существующие FK.

begin;

alter table id_form_status add column tab_id bigint references id_form_tab(id);
alter table id_form_status drop constraint id_form_status_code_key;
alter table id_form_status add constraint id_form_status_tab_code_key unique (tab_id, code);

commit;
