-- Обнаружено при первой реальной загрузке (15.08.2026): колонка "Объём работ"
-- на исходном листе иногда содержит текст вида "70шт" вместо числа —
-- тот же класс дефекта, что уже обрабатывался для work.fact_pct/fact_pct_raw
-- (ТЗ 11.4.2), просто в другом поле. Не переписываем 001_init.sql задним
-- числом — он уже применён; это отдельная миграция поверх.

begin;

alter table work add column volume_raw text;

commit;
