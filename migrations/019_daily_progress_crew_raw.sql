-- Задание координатора 29.08.2026 (блокер к запуску 01.09): текстовые
-- значения плановой/фактической численности ("ПОДРЯД"/"Подряд" —
-- работу ведёт субподряд, число людей неизвестно) раньше молча
-- превращались в NULL при загрузке (load_to_postgres.py:
-- `crew = val if isinstance(val, (int, float)) else None`). "ПОДРЯД" —
-- это не отсутствие данных, а конкретный, отдельный факт («число людей
-- неизвестно, работу ведёт подрядчик») — не 0 и не выдуманное число.
-- Хранится рядом с числовым полем, тот же принцип, что fact_pct/fact_pct_raw.

alter table daily_progress
    add column planned_crew_raw text,
    add column actual_crew_raw text;
