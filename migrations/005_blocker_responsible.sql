-- Экран "Что делать сегодня" (реинжиниринг v3, Цикл 2): ограничениям нужен
-- ответственный и срок снятия, чтобы список был actionable, а не просто
-- списком текста. owner_id (FK на app_user) не годится — в app_user
-- реально есть только общий технический вход веб-формы (см.
-- REENGINEERING_LOG.md), полноценного справочника сотрудников ПТО нет и
-- создавать его отдельной задачей не просили. Поэтому — простой текст.

begin;

alter table blocker add column responsible_name text;

commit;
