-- Стоп-фактор в форме контура ИД (/id-entry) до сих пор писался как
-- blocker_type='design_decision' — ближайшее из существующего enum, но
-- семантически не то (design_decision — про СМР, отсутствие проектного
-- решения по конструктиву). В проекте уже была находка (04.07.2026,
-- «дождь + отсутствие ГСМ» одним типом «погода»), что смешение
-- разнородных причин в одном типе портит аналитику по простоям —
-- не повторяем её здесь, заводим отдельное значение.

alter type blocker_type add value 'id_docs';
