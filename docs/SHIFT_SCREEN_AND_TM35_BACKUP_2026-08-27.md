# Экран смены + резервирование tm35 — отчёт (27.08.2026)

## 1. Экран смены (`/shift`) — сеточный ежедневный ввод факта

Задача координатора: заменить одиночную форму `/form` сеткой, чтобы
инженер ПТО мог полностью отказаться от Excel-графика для ежедневного
факта. Построено, задеплоено на `tm.asd-kontur.ru`, проверено вживую.

### Разведка перед разработкой

- **`POST /api/gantt/cell`** — не построчный инлайн-грид, а модалка:
  клик по дню открывает окно на все поля разом (план/факт людей,
  причина, комментарий), одно сохранение. Апсерт в `daily_progress` по
  `(date, work_id, source='web_form')`, `audit_log.action='gantt_cell_edit'`.
- **`POST /form`** — тот же ключ апсерта; дополнительно при непустом
  `fact_pct` обновляет `work.fact_pct` (читают `/dashboard`, `/gantt`,
  `/works`). `audit_log.action='web_form_submit'`.
- **«Работа актуальна на дату» — проверено данными, не предположено.**
  Наличие строки в `daily_progress` за последние 7 дней есть у 165 из
  165 работ (артефакт календарной матрицы Excel-импорта) — не сигнал.
  `baseline_schedule` с уверенностью high/medium тоже не годится
  фильтром по умолчанию — у 42 из 114 незавершённых работ такого
  baseline нет вовсе, фильтр по нему запер бы именно тех, кому нужно
  отчитаться. Выбран фильтр `work.status not in DONE_STATUSES` — тот
  же критерий, что уже использует переключатель «только активные» на
  `/gantt` → 114 из 165 работ по умолчанию, «показать все» → 165.

### Что построено

`/shift` (страница) + `GET /api/shift` (чтение) + `POST /api/shift/cell`
(сохранение) + переиспользован существующий `POST /api/gantt/work` для
добавления работы прямо с экрана.

- Дата вверху (по умолчанию сегодня), смена перезагружает сетку.
- Колонки: шифр · наименование · участок · % готовности · люди ·
  простой (тот же `REASON_CODES` из `main.py`) · комментарий · статус.
- **Частичное сохранение по колонке** — каждое поле сохраняется своим
  upsert'ом, `SET`-часть трогает только свою колонку. Несколько
  инженеров ПТО могут одновременно редактировать разные поля одной
  строки, не затирая друг друга — гарантия на уровне SQL, проверено на
  живых данных (бэкдейт-запись содержала только `fact_pct`, остальные
  поля `NULL`).
- Клавиатура: Tab/Shift+Tab — нативный порядок DOM. Enter/↓/↑ —
  сохраняют и двигают фокус по той же колонке. ←/→ — переход в соседнюю
  колонку только когда курсор в начале/конце текста. Стрелки на
  `<select>` намеренно не перехватывались — там нативное переключение
  значения важнее.
- Статус строки: не заполнено / заполнено (зелёная плашка, время
  последнего изменения в тултипе) / сохранение… / ошибка (текст ошибки
  сервера в тултипе, инпут не сбрасывается).
- Placeholder — текущий `work.fact_pct`, если за день ещё ничего не
  введено. Чисто информационно, не отправляется.

**Решение, принятое самостоятельно (не переспрашивал):** колонка
«Люди» — факт (`actual_crew`) за смену, не пара план/факт. Планирование
людей остаётся на `/gantt` и в `/form`. Если имелось в виду другое —
поле легко расширить, апсерт уже параметризован по колонке.

### Проверка (живым браузером, Playwright, `1366×768`, реальный Basic Auth)

- Деплой сверен `docker exec` (не диском сервера).
- Скриншот после hard-reload — реально просмотрен: строки таблицы,
  пустые поля ввода, статус «не заполнено» серым; после правки —
  зелёная полоса слева на строке, значения на месте, статус «заполнено».
- Клавиатура пройдена по-настоящему: Tab между колонками одной строки;
  Enter переводил фокус на следующую строку той же колонки; ArrowUp —
  обратно.
- Сохранение пережило полный `reload()` (не только `render()`);
  `source='web_form'` в БД подтверждено запросом.
- Бэкдейт (сегодня−5 дней) — независимая пустая запись на ту же
  работу, другую дату, заполнена и сохранена отдельно от сегодняшней.
- Добавление работы с `/shift` — реально создало строку в `work`
  (`TM35-AUX-147`), после проверки удалена.
- Регресс — 13 маршрутов (`/dashboard /today /report /blockers /form
  /shift /id-packages /changes /prescriptions /gantt /data /critical
  /works`) — все 200, 0 консольных ошибок, 0 сетевых 4xx/5xx.
- Найден и исправлен до сдачи: Enter/стрелки вызывали двойное
  сохранение одного значения (лишняя запись в `audit_log`, данные не
  терялись) — поправлено оптимистичной пометкой «отправлено» перед
  запросом, перепроверено.
- Все тестовые записи (`daily_progress` за 2 даты, тестовая работа,
  связанные `audit_log`) удалены с `select` до/после.

### Не чинилось в рамках задачи (как и просил координатор)

Расхождение `REASON_CODES`/`downtime_cause`, английские статусы на
`/changes`/`/prescriptions`, `None` в подписи на `/report`, отсутствие
формы стоп-фактора, отсутствие ролей/авторизации.

---

## 2. Резервирование базы `tm35`

### Находка (до правки, 27.08.2026 днём)

`/opt/kat/backup.sh` дампил **только `kat_core`**
(`pg_dump -U kat_admin kat_core`, ежедневно 03:00, `kat_core_*.sql`,
заливка на Я.Диск). База **`tm35` не резервировалась вообще** — ни
строки в скрипте, ни файла в `/opt/kat/backups/`, кроме одного
разового ручного дампа (`tm35_20260827_030305.dump`, 550 КБ, снят
вручную кем-то из предыдущей сессии, без повторения).

Пока рядом есть Excel — он де-факто второй экземпляр данных. После
отказа от Excel единственный экземпляр — база, у которой не было
резервной копии.

### Правка

`/opt/kat/backup.sh` дополнен веткой `tm35` (custom-формат `-Fc`,
как у единственного ручного дампа): `pg_dump -Fc tm35` →
`/opt/kat/backups/tm35/tm35_${DATE}.dump` → `rclone copy` на
`yadisk:postgres_backup/tm35/` → недельная ротация. Старый скрипт
сохранён как `/opt/kat/backup.sh.bak.20260827_before_tm35`. Синтаксис
проверен (`bash -n`), связка дамп→заливка проверена вручную сухим
прогоном — сработала, тестовый файл удалён и локально, и с Я.Диска.

### Статус на 27.08.2026 16:55 MSK — резервируется ЧАСТИЧНО

Механизм внедрён и вручную подтверждён рабочим end-to-end, но
**ни одного самостоятельного, кроном порождённого дампа `tm35` ещё
нет** — правка внесена днём, следующий пуск `crontab`
(`0 3 * * * /opt/kat/backup.sh`) — ближайшие 03:00 MSK, ещё не
наступили на момент проверки. Наличие строки в crontab само по себе не
доказательство успешного выполнения.

**Проверить после 03:00 MSK**: `ls -la /opt/kat/backups/tm35/` должен
показать новый файл со свежей датой (не только старый от 27.08 03:03).
Если файла нет — значит cron либо не запустился, либо упал молча,
самостоятельная проверка станет следующей задачей.

---

## 3. Восстановление из дампа `tm35` — проверено (28.08.2026)

Единственный существующий дамп (`tm35_20260827_030305.dump`) реально
восстановлен `pg_restore` в отдельную scratch-базу `tm35_restore_test`
на том же Postgres — без ошибок. Счётчики `work`=165, `id_package`=306,
`blocker`=9 совпали с боевыми день в день; `daily_progress`=4682 против
4684 живых — расхождение в 2 строки объясняется временем, прошедшим с
момента снятия дампа (27.08 03:03), не повреждением. Scratch-база
удалена сразу после проверки (`select` до/после подтверждают).

Кроновый (автоматический) прогон обновлённого `backup.sh` по состоянию
на 27.08 17:09 MSK ещё не наступал (следующий — 03:00 MSK). Итог по
`tm35`: **дамп снимается и рабочий, восстановление из него рабочее,
но самостоятельного кронового прогона ещё не было** — резервирование
частичное до первого автоматического запуска.

---

## 4. Форма создания стоп-фактора (`/blockers`)

Находка из брифинга (§6.5/§6.6): `insert into blocker` в коде
отсутствовал вообще — все 9 существующих строк попали разовым
импортом, а `POST /api/blocker/{id}` умел только снять существующую
запись или поменять срок/ответственного.

Добавлено: `POST /blockers` — по образцу уже существующего парного
`GET/POST /changes` (тот же паттерн: одна страница, форма сверху,
реестр снизу, полная перезагрузка + redirect + флэш, без AJAX — эта
страница проще `/gantt`/`/shift`, ajax тут не нужен).

- Поля: работа (опционально, `work_id` может быть `NULL` — как и у
  существующих строк дневного уровня), тип (`blocker_type`, обязателен,
  сверен с реальными значениями enum в БД — 13 штук, совпадают с
  `RU_BLOCKER_TYPE`), описание (обязательно), ожидаемая дата снятия
  (опционально), ответственный (опционально).
- Новая запись всегда создаётся со `status='active'` — до сих пор статус
  не задавался при создании нигде в коде.
- `audit_log.action='blocker_create'` — этого пути раньше не было ни у
  одной вставки `blocker`.
- Не трогал `POST /api/blocker/{id}` (снятие/перенос срока) — оставлен
  как есть.

**Проверено:**
- Форма реально создала запись (`select` до/после, id=217) с корректным
  `status='active'`, `owner_id`, и парную запись в `audit_log`.
- Серверная валидация проверена **в обход браузера** (`curl -X POST` с
  пустыми полями) — обе обязательные ошибки («Тип» и «Описание»)
  вернулись текстом на странице, HTTP 200 без редиректа, запись не
  создана.
- **Найден и исправлен сам, до сдачи**: `GET /blockers` не читал
  параметр `?ok=1`, которым редиректит успешное создание — плашка
  «Сохранено.» никогда не показывалась. Добавлен параметр `ok: str = ""`,
  перепроверено `curl` — плашка теперь рендерится.
- Регресс — все 13 маршрутов (включая `/shift` из предыдущего пункта)
  — 200, 0 ошибок, 0 битых запросов.
- Тестовая запись и её `audit_log`-запись удалены, счётчик `blocker`
  вернулся к 9.

---

## 5. Выгрузка в CSV с реестров (28.08.2026)

Из документа «Критерии готовности к запрету Excel» (§2): «чтобы
отказаться от Excel как источника, нужно дать Excel как выгрузку —
иначе первое же «пришлите в таблице» вернёт людей обратно». Сделаны
все 5 явно перечисленных там реестров.

- `GET /export/works.csv` — реестр работ, с теми же фильтрами, что на
  `/works` (источник/статус/исполнитель/наименование) — выгружает то,
  что видно на экране, не всегда весь реестр целиком. Кнопка на `/works`.
- `GET /export/daily-progress.csv?date_from=&date_to=` — факт за
  произвольный период (через `LATEST_DP_CTE`, та же «текущая истина»,
  что видит остальной интерфейс). По умолчанию — последние 30 дней.
  Форма с двумя `dm-picker` добавлена на `/report`.
- `GET /export/id-packages.csv`, `/export/blockers.csv`,
  `/export/prescriptions.csv` — полные реестры, ссылки на своих
  страницах.

**Технические решения:**
- UTF-8 с BOM (`﻿` в начале файла) — без него Excel на Windows
  показывает кириллицу битой при открытии двойным кликом.
- Разделитель `;`, не `,` — в русской локали Excel только с `;`
  автоматически разбивает столбцы при открытии по клику, без ручного
  «Данные → Текст по столбцам».
- Даты — `ДД.ММ.ГГГГ` (через общий `_csv_dmy()`), как того требует
  правило проекта про формат дат в интерфейсе — CSV для человека тоже
  интерфейс, ISO в нём не показывается нигде.
- Статусы `/changes`/`/prescriptions` (`change` пуст, `prescription`
  пуст на 28.08) намеренно оставлены как есть (сырые английские значения) —
  это уже известная находка (см. брифинг §6.4), не расширял её здесь.

**Проверено:**
- Все 5 эндпоинтов — `curl` с реальной Basic Auth, HTTP 200, верный
  `Content-Type: text/csv; charset=utf-8`, верный `Content-Disposition`.
- Реальное содержимое проверено построчно: `works.csv` — 165 строк
  данных + заголовок; `daily-progress.csv` (по умолчанию 30 дней) —
  реальные записи с русскими подписями причин простоя и источника;
  `id-packages.csv` — 306 (многострочные поля `Состав` корректно
  заключены в кавычки CSV-модулем, не ломают структуру);
  `blockers.csv` — 9; `prescriptions.csv` — 0 строк данных (реестр
  пуст, заголовок присутствует — не падает на пустой таблице).
- Даты перепроверены после правки — `26.08.2026` вместо `2026-08-26`
  во всех выгрузках, включая время (`19.08.2026 13:59`).
- Кнопки/ссылки реально просмотрены на скриншотах после hard-reload на
  `/works`, `/report`, `/blockers` — на месте, стиль не ломает разметку.
- Регресс — все 13 маршрутов 200, 0 ошибок, 0 битых запросов.

---

## 6. Форма плановых сроков (`/baseline`) — 28.08.2026

`baseline_schedule` (146 из 165 работ) до сих пор правилась только
разовым импортом (миграция `003_baseline_source.sql`). Питает
«Успеваем», «Сегодня», «Критичные работы» через `confidence in
('high','medium')`.

- **Миграция `011_baseline_source_web_form.sql`** — добавлено значение
  `'web_form'` в enum `baseline_source`. До правки источник знал только
  `matrix_schedule`/`text_month_only`/`no_data` (все — про исходный
  Excel-импорт); запись, введённая человеком, промаркировать одним из
  них значило бы соврать про происхождение даты. `'web_form'` — то же
  имя, что уже устоялось в `daily_progress.source`/`id_package`, не
  новая идея.
- `GET/POST /baseline` — форма (работа, начало, окончание, плановая
  численность, уверенность, комментарий) + таблица всех 165 работ.
  Нет уникального ограничения на `baseline_schedule.work_id`
  (проверено `pg_constraint`) — обновление/вставка разведены в коде:
  если для работы уже есть строка, `UPDATE` по её `id`, иначе `INSERT`.
- **Найден и исправлен сам, до сдачи**: ссылка «добавить» у работ без
  baseline использовала `bs.work_id` — а это колонка из `LEFT JOIN`,
  для работ без строки в `baseline_schedule` она `NULL`, ссылка вела на
  `/baseline?work_id=None`. Причина — работа выбиралась не из `w.id`.
  Исправлено (`w.id as work_pk`), перепроверено на реальной работе.
- **Проверено**: создание новой записи (id=1061, `confidence=medium`,
  `baseline_source=web_form`) — подтверждено в БД и `audit_log`;
  редактирование той же записи (смена `confidence` на `high`) —
  подтверждено, что это `UPDATE` той же строки, а не второй `INSERT`
  (был 1 ряд, остался 1 ряд, тот же `id`). Тестовые данные удалены с
  `select` до/после (запись + 2 связанные `audit_log`).

## 7. Реестр субподрядчиков (`/subcontractors`) — 28.08.2026

`subcontractor` (0 строк) — таблица существовала, `insert` в коде не
было вообще, показывались только работы с `executor_type='subcontract'`
(без привязки к юрлицу, эта проекция не тронута). Добавлена форма
(организация, вид работ, статус договора/мобилизации, бригада, начало
план/факт, причина задержки, влияние, комментарий) —
`POST /subcontractors`, по образцу `/changes`/`/blockers`.
Проверено: реальная запись создана и видна в реестре сразу после
сохранения (`select` в БД + флэш «Сохранено.» на странице), удалена
после проверки вместе с `audit_log`.

## 8. Настройки объекта (`/settings`) — 28.08.2026

Директивный срок и координаты объекта менялись только прямым запросом
в БД. Существовавший `POST /api/settings/directive-deadline`
рассчитан на форму на странице `/status` — а `templates/status.html` в
контейнере отсутствует, `/status` ни в одном пункте меню не
упоминается (мёртвый маршрут более раннего цикла реинжиниринга, см.
раздел 9 ниже). Ни этот эндпоинт, ни `/status` не тронуты («не
чистить»); добавлена новая работающая страница `/settings`
(директивный срок + широта/долгота), сохраняет полной перезагрузкой.

**Проверено — реальная сквозная интеграция, не только запись в БД**:
изменил директивный срок через форму на `30.09.2026` → значение в
`app_setting` обновилось → `/today` немедленно показал «директивный
срок объекта: 30.09.2026» (та же строка, что видит инженер). После
проверки значение возвращено на исходное `24.08.2026`, `/today`
перепроверен — снова показывает исходную дату. Тестовые данные
(субподрядчик, `audit_log`) удалены с `select` до/после.

## 9. Комплексная проверка после завершения очереди (28.08.2026)

Полный обход всех человеко-читаемых маршрутов (27, включая только что
добавленные), не только тех, что были в предыдущих регрессах:

- **25 из 27 — 200, 0 консольных ошибок, 0 битых сетевых запросов.**
- **Два маршрута отвечают 500 — `/losses` и `/status`.** Оба —
  `jinja2.exceptions.TemplateNotFound` (`losses.html`/`status.html`
  отсутствуют в `/app/templates` контейнера, подтверждено логами
  контейнера). Оба — не находка этой сессии по существу задачи и не
  трогались: **не в текущей навигации** (`grep` по всем шаблонам на
  `href="/losses`/`href="/status"` — 0 совпадений), значит не
  всплывают ни в одной живой ссылке интерфейса, только при прямом
  вводе URL. Судя по комментариям в `main.py` рядом с `/status`
  («упрощённая форма simple.html удалена», «редирект на панель
  координатора») — хвосты более раннего цикла реинжиниринга, шаблоны
  удалены, обработчики в `main.py` — нет. Не удалял и не чистил
  (правило проекта) — зафиксировано находкой, чинить не входило в
  периметр задачи.
- **Найдена и исправлена находка по себе**: при уборке тестовых данных
  после проверки экрана смены (раздел 1 этого файла) была удалена
  тестовая строка `daily_progress`, но не связанные с ней 6 строк
  `audit_log` (`shift_cell_edit`, id 143–148) — обнаружено только на
  этом комплексном проходе по логам `audit_log`. Удалены сейчас, с
  `select` до/после.
- **Побочно замечено, не трогалось**: в `audit_log` остаются записи
  `web_form_submit`/`gantt_cell_edit` с явно тестовыми значениями
  (`TestV2Final`, id 134–142, метка времени раньше начала этой сессии)
  — не моя уборка, чужой тестовый след из более раннего цикла. Не
  удалял — не могу подтвердить, что это не нужно кому-то другому, не
  моя находка по времени/сессии.
- Итоговые счётчики таблиц после всей работы этой сессии — сверены с
  начальными значениями брифинга один в один (`work`=165,
  `daily_progress`=4684, `id_package`=306, `change`=0,
  `prescription`=0, `blocker`=9, `baseline_schedule`=146,
  `subcontractor`=0, `app_setting`=4, `audit_log`=129) — никакой
  тестовый шум не остался.
- Диск сервера — 33 ГБ свободно из 77 ГБ (58% занято), контейнер
  `tm_backend` — 45 МиБ памяти, 0.23% CPU в покое — без аномалий.
