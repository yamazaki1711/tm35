# TM-35 — заход 3, 10.09.2026

Правила те же (см. `docs/NIGHT_RUN_20260909.md` §0 и оба предыдущих
файла). Отдельный файл — по той же причине, что и раньше: предыдущий
(`docs/RUN_20260910.md`) уже закрыт итоговой таблицей, не смешивать.

**Если сессия оборвалась** — читать этот файл, искать первую задачу не
в статусе ГОТОВО/ОТКАЗ, продолжать с неё.

## Ответы координатора на открытые вопросы прошлого захода — закрыто, без изменений в коде

- **Конвенция периодов матрицы** (60 периодов, 10.03.2026-01.06.2027,
  блоки внутри месяца, границы обрезаны по датам периода) — подтверждена
  как построено. Изменений нет.
- **Определение ячейки матрицы** — узкое прочтение подтверждено:
  именно передача на «Первичная проверка РСК», не весь цикл РСК.
  Обоснование координатора: совпадает с определением жёлтого цвета в
  той же матрице и с плитками 4-6 блока 1 (о том же событии). Изменений
  в коде нет — пункт закрыт.
- **Две мёртвые VIEW (`v_blocked_amounts`, `v_id_dashboard`)** — решение:
  удалить миграцией. Обоснование: «не чистить» защищает данные (таблицы/
  колонки с настоящими значениями), не производный объект с нулём
  ссылок, читающий поле, которому приложение само перестало доверять.
  См. задачу 2 ниже.

## Состояние очереди

| # | Задача | Статус | Коммит | Начато | Завершено |
|---|--------|--------|--------|--------|-----------|
| 0 | Закрыть 3 открытых вопроса (без кода) | ГОТОВО | — | 14:00 | 14:05 |
| 1 | Массовый ввод денег по папкам ИД | ГОТОВО | 95ec182 | 14:05 | 15:15 |
| — | Внеочередное: пересборка "Обзор ИД" на /dashboard вокруг папок (сообщение координатора в процессе) | ГОТОВО | 95ec182 | 14:40 | 15:15 |
| 2 | Удалить 2 мёртвые VIEW | ГОТОВО | b5c7574 | 15:15 | 15:30 |
| 3 | Убрать legacy-форму «Предписания РСК» из модуля ИД | ГОТОВО (уже сделано раньше) | 688fbe0 | 15:30 | 15:40 |
| 4 | Связь ИД↔РСК + заполнение «Замеч. РСК» в экспорте | ГОТОВО | 9350a9f | 15:40 | 17:10 |
| 5 | РСК: логика закрытия по diff актов + тест | ГОТОВО | 0908233 | 17:10 | 17:40 |
| 6 | Дозагрузка очереди (проверки, playwright, аудит, документация) | В РАБОТЕ | — | 17:40 | — |

## Журнал хода

14:00 — заход начат, синхронизировал репозиторий с контейнером (diff
пуст). Закрыл 3 открытых вопроса по прямым ответам координатора (см.
выше) — записи не требуют кода, только фиксации. Начинаю задачу 1.

14:05-15:15 — задача 1, `/id-folders/bulk-entry`. Отладка заняла время:
виджет даты (`datepicker.js`) синхронизирует hidden-инпут с текстовым
только по `blur`/Enter, а не по каждому нажатию — первые попытки клика
через приблизительные координаты скриншота промахивались мимо поля
(screenshot — уменьшенная копия, реальный viewport в этой сессии втрое
шире; клик по `getBoundingClientRect()` в CSS-пикселях сработал верно).
Разобрался, поймал оба механизма, довёл до конца: реальная форма в
браузере, реальное сохранение, реальная проверка отказа сметной
стоимости без даты подписания, откат тестовых данных на ТМ-002 после
проверки. Коммит `95ec182` (вместе со следующей записью).

14:40 — координатор прислал сообщение с внеочередной задачей прямо во
время отладки задачи 1: блок "Обзор ИД" на /dashboard визуально не
отражал модель данных прошлой ночи (раздел — крупная плитка наверху,
папка — мелкие плитки ниже, хотя платят за папку). Прямое указание:
"задача по интерфейсу закрыта не когда есть код, а когда видно на
отрендеренной странице" — прервал отладку задачи 1 (была close to
done, безопасно отложить), сделал вставку, вернулся и договорил
задачу 1. Обе части — в одном коммите `95ec182` (main.py менялся для
обеих в одном промежутке времени — резать диффы по живому не стал, тот
же принцип, что и в задачах 2-3 прошлого захода). Полное описание
вставки — в самом коммите. Регрессионная сеть выросла до 40 проверок.

15:15 — начинаю задачу 2 (удалить 2 мёртвые VIEW). Решение координатора
уже есть (см. раздел выше) — сначала повторная проверка нулевых ссылок
перед миграцией, как и предписано ("Verify zero references one more
time before dropping").

15:30-15:40 — задача 3. Проверка (не со слов задания) показала: форма
`/prescriptions`, её маршруты и экспорт уже убраны координатором
06.09.2026 (`main.py:3229-3233`, `KNOWN_ISSUES.md` §28/§30/§31), именно
по причине, названной в задании (дублирует РСК). `GET /prescriptions`
на сервере — 404, таблица `prescription` — 0 строк, в шаблонах и меню
следов нет. Единственный остаток — `prescriptions:submit` в
справочнике зонных прав, оставленный НАМЕРЕННО (решение координатора
30.08.2026, не про эту форму). Действий не потребовалось, записано в
`KNOWN_ISSUES.md` §35, коммит `688fbe0`. Начинаю задачу 4.

15:40 — задача 4 (связь ИД↔РСК). Разбор перед кодом: нужна таблица
многие-ко-многим `rsk_processing` ↔ `id_form_row`, экран прикрепления/
открепления (без автосопоставления — только руками), и вписать
заполнение колонок «Замеч. РСК» в экспорт «График ИД» для групп, чьи
разделы имеют прикреплённые замечания.

15:40-17:10 — задача 4 готова, коммит `9350a9f`. Отклонение от
буквального текста задания записано и обосновано в самом коммите
(rsk_processing -> rsk_violation, см. также миграцию 034). Полный цикл
(прикрепить/открепить через реальный экран, экспорт с непустой и с уже
занятой ячейкой P) проверен на живых данных, тестовые связи убраны.
tools/check_consistency.py — 40/40 без изменений.

17:10 — начинаю задачу 5. Разбор перед кодом: сейчас в БД только один
акт РСК (`rsk_act`, 1 строка), диффить не с чем — задание прямо
предписывает не грузить синтетику в боевую БД, тест строит и
выбрасывает данные сам. План: чистая функция diff(act_n_violations,
act_n1_violations) -> закрытые sys_no, без обращения к БД внутри самой
функции (легко тестировать); отдельная функция apply_act_diff_closure
(или расширение существующего импорта акта), которая ВЫЗЫВАЕТ diff и
пишет closed_in_act_id — она НЕ тестируется синтетикой (это и была бы
запись в боевую БД), тестируется только чистая функция.

17:10-17:40 — задача 5 готова, коммит `0908233`. Находка перед тестом:
diff-закрытие уже реализовано и живое (`_rsk_diff_vs_previous` +
`/rsk/import/confirm`), просто ещё ни разу не сработало (1 акт в БД).
Попутно исправлено реальное расхождение того же класса, что ищет аудит
08.09.2026: предпросмотр и подтверждение считали "снятые" нарушения
двумя независимыми способами — свёл к одному (`diff["removed"]`).
Тест — 8/8, БД не тронута (числа актов/нарушений сверены до/после).
tools/check_consistency.py — 40/40. Начинаю задачу 6 (дозагрузка).

17:40-18:00 — задача 6, пункт 1 готов (расширение check_consistency.py,
коммит `8df0c8e`). Пункт 2: playwright реально установлен в
контейнере (`pip install playwright` + `playwright install chrome` —
скачал и настроил настоящий Google Chrome с системными зависимостями,
~500 МБ, `/root/.cache/ms-playwright` + `apt`-пакеты; это состояние
контейнера, не образа — переживёт `docker restart`, но НЕ переживёт
пересборку из Dockerfile, в `requirements.txt` playwright не добавлен,
решение добавлять или нет — координатору, если хотят гонять эти
сценарии регулярно). Обнаружено: `functional_test.py` требует Basic
Auth (`TM_BASIC_AUTH_USER/PASSWORD`), но живой сайт сейчас НЕ запрашивает
Basic Auth вообще (проверено `curl` без креденшлов — 200 напрямую,
без 401). Файла `.secrets/tm_basic_auth.env` нигде на сервере нет.
Прогнал 3 сценария отдельным эпемерным раннером (не коммитился,
`sys.path` на `tools/functional_test.py`, `browser.new_context()` без
`http_credentials` — раз сайт их сейчас не спрашивает) — **все три
прошли**:
- «переключение вкладки в матрице меняет строки» — ОК ('ОПн1' -> 'ОПн15')
- «клик по разделу открывает drill-down без перезагрузки» — ОК
- «/id-folders: счётчики видны и это числа» — ОК

Существующие ~20 сценариев самого `functional_test.py` (СМР-контур,
Гантт, календарь и т.д.) не запускал — задание просило именно эти три,
не полный прогон всего файла.

18:00-18:15 — задача 6, пункт 3 (продолжение аудита статичных полей).
Прошёл `information_schema` на колонки `*_count/_total/_sum/_pct` по
всей схеме — всё найденное (`resource_pool.actual_total/available_total`,
`resource_reallocation.people_count`, `norm_plan_item.labor_hours_total`,
`daily_progress.fact_pct`, `work.fact_pct`) — легитимно введённые
данные, не дублирующиеся вычисляемые агрегаты. Отдельно перепроверил
`amount_rub` по всему `main.py`/шаблонам — каждое использование либо
`id_manual_volume.amount_rub` (сознательно входит в `manual_sum`), либо
`id_folder.amount_rub` (сознательно исключено из денежных фигур,
подписано как "Оценка ПТО"), либо `change.blocked_amount_rub` — другое
поле другой таблицы, не тот же баг. Хардкодов "66"/"163" (старые
агрегаты ИЗМ/предписаний, п.10 старого decisions_needed) в шаблонах не
нашёл — тоже чисто. Новых находок в этом заходе аудита нет — отдельно
от находки §33 (мёртвые VIEW) в начале этого захода.

18:15 — задача 6, пункт 4 (документация) — см. следующий коммит.
