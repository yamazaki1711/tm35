# Текущее состояние работ

Обновлено: 15.09.2026

## Сейчас в работе
Настройка выживания проекта при перезапуске сессии (эта задача): импорт
`CLAUDE.md` на пути запуска, этот файл, дисциплина обновления. Сразу
следующим шагом в очереди (без паузы) — пересборка блока «Обзор ИД» на
`/dashboard` по ТЗ Якименко А.И. от 15.09.2026 (заголовок задачи в
координаторском задании: «rebuild the «Обзор ИД» block»).

## Последнее сделанное
- `77ff292` — CLAUDE.md: деплой-правило прогнано как репетиция, шаг 1 исправлен.
- `654d6fb` — main.py: печатать запущенный коммит в лог при старте (`DEPLOYED_COMMIT`).
- `fe898e5` — KNOWN_ISSUES §41: записи без вида работ скрывают работу на «Электрике».
- `9c49ba8` — CLAUDE.md: правило «деплой из git в контейнер, не наоборот».
- `e34a5c3` — Обзор (/dashboard): экран Заказчика, три блока вместо развёрнутых секций.
- `1bc34ed` — синхронизация с продакшеном 8 файлов, разошедшихся за прежние сессии
  (`docs/PROD_SYNC_2026-09-15.md`).

## Следующее
1. Пересборка блока «Обзор ИД» на `/dashboard` по ТЗ Якименко от 15.09.2026
   (в работе, см. выше).
2. Первичные документы РСК/ИЗМ (акт №4183-156, построчные реестры) — на
   Google Drive физически нет, запрошены у ССР/РСК напрямую; до получения
   разделы «Предписания»/«Изменения» на `/closure` остаются заглушкой.
3. КЗ-40/КЗ-41 из `KNOWN_ISSUES.md` (см. ниже) — ждут решения координатора,
   не блокируют очередь.

## Требует решения координатора
- KNOWN_ISSUES §41: 23 записи `id_form_entry` без вида работ прячут разделы
  (у «Электрики» 4 из 6 разделов с данными не видны на `/id-progress`) —
  рекомендация дана, правило подсчёта не менялось до ответа.
- KNOWN_ISSUES §40: глобальный `th { font-size: .72rem }` (11.52px) ниже
  пола проекта 14px на заголовках всех таблиц сайта — правка задела бы
  вёрстку всех таблиц разом, не только одной задачи.
- KNOWN_ISSUES §40: подпись недели на `/id-progress` — диапазон
  (`10–16.08.2026`) вместо общего правила «дата понедельника» (CLAUDE.md,
  раздел «Интерфейс») — сделано сознательно из-за обрезанных крайних колонок,
  но расходится с остальным сайтом.
- `docs/PROD_SYNC_2026-09-15.md` п.5: `pdfplumber`/`openpyxl` есть в
  `requirements.txt` в git, но не установлены в живом образе — риск потери
  при пересборке `--no-cache`, не проверялось глубже.

## Задеплоено
`654d6fb` в проде на 15.09.2026 (на один коммит позади `origin/main` —
разница чисто документная, правки кода нет). Сверка контейнер/git —
15.09.2026, `check_consistency.py` 69/69 без расхождений.
