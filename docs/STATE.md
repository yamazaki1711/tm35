# Текущее состояние работ

Обновлено: 17.09.2026

## Сейчас в работе
Ничего не в работе — задачи 1 (ИД) и 2 (РСК) из 3 по ТЗ Якименко А.И.
16.09.2026 закрыты и задеплоены, см. ниже. Задача 3 (СМР) придёт
отдельным заданием.

## Последнее сделанное

### Задача 2 — РСК (ТЗ Якименко А.И., 16.09.2026)
- **«Обзор РСК» — шесть тайлов**, идентичных на `/dashboard` и
  `/rsk/dashboard` (единый источник `compute_rsk_dashboard_stats()`):
  `Активных замечаний` · `Готовы к снятию` · `Предъявить ИД` ·
  `Корректировка ПД/РД` · `Отклонено РСК` · `Устранено` (новый).
- **«Устранено» — слой 2** (`rsk_processing.resolved`/`resolved_date`,
  чекбокс + дата в форме `/rsk/processing`). НЕ трогает
  `rsk_violation.is_active`/`closed_in_act_id` — те остаются только за
  импортом акта (правило двух слоёв не нарушено). Нарушение может быть
  «Устранено», но ещё присутствовать в последнем акте — не ошибка,
  показывается как есть.
- **«ИКС» скрыта** (`rsk_responsible.active=false`), не удалена — 11
  записей `rsk_processing_responsible` уже ссылались на неё
  (диагностика ДО правки). Скрыта из формы отработки и фильтра реестра;
  там, где уже реально записана, имя остаётся видимым (см.
  `KNOWN_ISSUES.md` §47 — прямое противоречие в самом задании,
  разрешено консервативно). «Стройка ТМ-35» → «строй-площадка» одной
  правкой в справочнике.
- **«Физика»** — аббревиатуры раскрыты (`не треб.`→`не требуется` и
  т.д.), `факт (нужна корректировка РД)` убран из выбора (легаси-строки
  отображаются). `needs_rd` переведён с `track_phys='fact'` на трек
  «Проект» (`track_design='not_done'`, легаси `fact` считается тоже) —
  тайл вырос с 32 до 35 (`KNOWN_ISSUES.md` §48).
- **«Условие снятия»** — слово «неделя» убрано из всех пяти подписей
  `rsk_close_condition`.
- **Реестр**: убраны дропдауны «Акт»/«Повторность»; «Состояние»
  получил исправленную `selected`-логику (была визуально сломана);
  разведены колонки «Снято» (структурно, было по ошибке подписано
  «Устранено») и «Устранено» (слой 2, новая).
- **«Загрузка акта» убрана из меню РСК и с `/rsk/dashboard`** — маршрут
  `/rsk/import` остаётся рабочим по прямой ссылке:
  **`https://tm.asd-kontur.ru/rsk/import`** (не терять — не в меню,
  открывать по этому URL или из закладки). Проверено 17.09.2026:
  загрузка существующего акта дала корректный предпросмотр/диф.
- См. `docs/RUN_20260917_rsk_section.md`.

### Задача 1 — ИД (ТЗ Якименко А.И., 16.09.2026)
- «Стоимость» унифицирована в одно слово «Стоимость, ₽» везде
  (экраны/CSV), значение — `coalesce(amount_smeta_rub, amount_rub)`
  через `id_folder_cost()`. Пять денежных тайлов, идентичных на
  `/dashboard` и `/id-folders` (новый «Подписано ранее, ₽», граница
  10.03.2026 в `app_setting`). «Папки» пересобраны: новый порядок
  секций, 6 колонок таблицы, 6 значений фильтра «Статус».
- См. `docs/RUN_20260917_id_stoimost_unification.md`.
- «Папки» и «Реестр папок» слиты в одну страницу `/id-folders` (задача
  до этой). См. `docs/RUN_20260917_id_folders_merge.md`.

## Следующее
1. Задача 3 (СМР) — ждёт отдельного задания координатора.
2. Первичные документы РСК/ИЗМ (акт №4183-156, построчные реестры) — на
   Google Drive физически нет, запрошены у ССР/РСК напрямую.
3. КЗ-40/КЗ-41/КЗ-43/КЗ-44/КЗ-46/КЗ-47/КЗ-48/КЗ-49/КЗ-50 из
   `KNOWN_ISSUES.md` (см. ниже) — ждут решения координатора, не
   блокируют очередь.

## Требует решения координатора
- KNOWN_ISSUES §47 (новое, 17.09.2026): «ИКС» технически видна на
  экране там, где уже реально записана (11 карточек РСК) — прямое
  противоречие между «нигде не видна» и «ни одна запись не теряет
  ответственного» в самом задании; разрешено в пользу сохранности
  данных, не запрашивая решения заранее.
- KNOWN_ISSUES §48 (новое): тайл «Корректировка ПД/РД» вырос с 32 до 35
  после перевода источника с `track_phys='fact'` на трек «Проект».
- KNOWN_ISSUES §49 (новое): значок статуса «Ждёт корректировки РД»
  (столбец «Статус» реестра) не переименован вслед за тайлом
  «Корректировка ПД/РД» — разные поверхности интерфейса.
- KNOWN_ISSUES §50 (новое): ссылка «Загрузить акт →» убрана и с
  `/rsk/dashboard`, не только из меню — расширение буквальной
  формулировки задания.
- KNOWN_ISSUES §45: тайлы «Подписано ранее, ₽»/«Подписано по КС-2, ₽»
  показывают честный 0,00 ₽ против упомянутых в этом файле ≈82 млн ₽
  на 08.04.2026 — решение, заносить ли историю задним числом.
- KNOWN_ISSUES §46: дропдаун «Статус» на `/id-folders` — нет пункта
  «Все» среди буквально заданных шести значений.
- KNOWN_ISSUES §44: шрифт < 14px сайтово в `.badge`/`code`/
  `.nav-label`/`.hotkey-hint`/`.kpi-label`/`.kpi-sub` — 2610 находок
  `ui_audit.py` на 17.09.2026 (рост от 2598 полностью объяснён новыми
  тайлами РСК того же класса, не регрессия). Поднять до 14px сайтово
  или завести отдельный пол для служебных мелких надписей — решение не
  моё.
- `/id-rsk-link`, `id_form_row.id=968`: `section_label='Опв1- УТ1'` —
  висящий дефис и строчная «п» в данных, не нормализовал молча.
- KNOWN_ISSUES §43: `reload_id_form_v2.py`/`reload_id_rows_v3.py`
  перезапишут ОПн197/ОПн198 при повторном запуске.
- KNOWN_ISSUES §42: резолюция внесена задачей 1 (единый остаток ИД) —
  «Текущая КС-2» в трубе по-прежнему кумулятивно, не по периоду,
  осталось открытым.
- `/id-grafik` существует отдельно от `/id-progress`, недостижима из
  меню ИД (пауза координатора 11.09.2026).
- KNOWN_ISSUES §41: 23 записи `id_form_entry` без вида работ прячут
  разделы у «Электрики» на `/id-progress`.
- KNOWN_ISSUES §40: подпись недели на `/id-progress` — диапазон вместо
  общего правила «дата понедельника».
- `docs/PROD_SYNC_2026-09-15.md` п.5: `pdfplumber`/`openpyxl` в
  requirements.txt, не установлены в живом образе.

## Задеплоено
`751edf443da745b07b87e41e6dea72bf39483553` в проде на 17.09.2026,
сверка контейнер/git — момент коммита (`docker logs tm_backend | grep
'[deploy]'` совпадает с `git rev-parse HEAD`). `check_consistency.py`
78/78 без расхождений (было 73 после задачи 1 в тот же день — 5 новых
проверок задачей 2). `ui_audit.py --no-shots`:
horizontal_scroll/off_viewport/clipped_cell — 0 везде; small_font —
2610, рост от 2598 объяснён новыми тайлами РСК (не регрессия).
