# Текущее состояние работ

Обновлено: 15.09.2026

## Сейчас в работе
Ничего не в работе — задача (пересборка «Обзор ИД») закрыта и
задеплоена, см. ниже. Следующее — из очереди «Следующее».

## Последнее сделанное
- (не запушено) `tools/check_consistency.py` — фикс `TypeError` в новой
  проверке трубы (`round(None,2)` при «— ₽»), уже задеплоен на прод,
  коммит будет создан этим же шагом сессии.
- «Обзор ИД» на `/dashboard` пересобран по ТЗ Якименко А.И. (ПТО) от
  15.09.2026 — деньги (4 тайла, новые `ks2_folders_sum`/
  `money_remaining_by_ks2`), труба переименована в «Выполнение» и
  получила своё 5-стадийное представление (`id_folder_pipe_stage()`,
  отдельно от канонической `id_folder_stage()`), разделы (4 тайла),
  навигация (4 карточки + тайл «Активных ИЗМ»). `/id-folders` → «Папки»
  везде, где называла страницу. См. `docs/RUN_20260915_id_overview.md`.
  Деплой: `9160383` в проде, `check_consistency.py` 68/68 без
  расхождений, все маршруты 200.
- `77ff292` — CLAUDE.md: деплой-правило прогнано как репетиция, шаг 1 исправлен.
- `654d6fb` — main.py: печатать запущенный коммит в лог при старте (`DEPLOYED_COMMIT`).
- `fe898e5` — KNOWN_ISSUES §41: записи без вида работ скрывают работу на «Электрике».
- `1bc34ed` — синхронизация с продакшеном 8 файлов, разошедшихся за прежние сессии
  (`docs/PROD_SYNC_2026-09-15.md`).

## Следующее
1. Первичные документы РСК/ИЗМ (акт №4183-156, построчные реестры) — на
   Google Drive физически нет, запрошены у ССР/РСК напрямую; до получения
   разделы «Предписания»/«Изменения» на `/closure` остаются заглушкой.
2. КЗ-40/КЗ-41/КЗ-42 из `KNOWN_ISSUES.md` (см. ниже) — ждут решения
   координатора, не блокируют очередь.

## Требует решения координатора
- KNOWN_ISSUES §42 (новое, 15.09.2026): два законных, но разных
  «остатка» — «Остаток по контракту, ₽» (/dashboard, от КС-2) vs
  «Остаток в деньгах, ₽» (/id-folders, от подписания). Свести к одному —
  отдельное решение.
- KNOWN_ISSUES §42: «Текущая КС-2» в трубе — кумулятивно за всю историю,
  не акт отчётного периода (в БД нет поля периода). Если ПТО имел в виду
  период — нужны новое поле и отдельная задача.
- `/id-grafik` существует отдельно от `/id-progress`, недостижима из
  меню ИД (пауза координатора 11.09.2026) — карточка «График ИД» ведёт
  на `/id-progress`, не переключал самовольно.
- KNOWN_ISSUES §41: 23 записи `id_form_entry` без вида работ прячут разделы
  (у «Электрики» 4 из 6 разделов с данными не видны на `/id-progress`) —
  рекомендация дана, правило подсчёта не менялось до ответа.
- KNOWN_ISSUES §40: глобальный `th { font-size: .72rem }` (11.52px) ниже
  пола проекта 14px на заголовках всех таблиц сайта — правка задела бы
  вёрстку всех таблиц разом, не только одной задачи.
- KNOWN_ISSUES §40: подпись недели на `/id-progress` — диапазон
  (`10–16.08.2026`) вместо общего правила «дата понедельника» — сделано
  сознательно из-за обрезанных крайних колонок, но расходится с
  остальным сайтом.
- `docs/PROD_SYNC_2026-09-15.md` п.5: `pdfplumber`/`openpyxl` есть в
  `requirements.txt` в git, но не установлены в живом образе — риск потери
  при пересборке `--no-cache`, не проверялось глубже.

## Задеплоено
`9160383` в проде на 15.09.2026 (совпадает с `origin/main` и с
`docker logs tm_backend | grep '[deploy]'`). `check_consistency.py`
68/68 без расхождений, сверка контейнер/git — 15.09.2026.
