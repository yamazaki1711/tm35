# Текущее состояние работ

Обновлено: 16.09.2026

## Сейчас в работе
Ничего не в работе — последняя задача (перенос текста по умолчанию +
`ui_audit.py`) закрыта и задеплоена, см. ниже.

## Последнее сделанное
- Инвертирован умолчательный перенос текста в таблицах: `td, th` теперь
  `white-space: normal` по умолчанию (было `nowrap`, перенос был opt-in
  через `.wrap` — отсюда третий подряд случай обрезки, `/id-rsk-link`).
  `.nowrap` — явный маркер атомарного значения, пройдены все 35
  шаблонов с `<table>`. `/id-rsk-link` приведён к паттерну
  `rsk_registry.html`/`rsk_processing.html` (превью по двум
  предложениям + подсказка по наведению). `tools/ui_audit.py` впервые
  реально задеплоен и прогнан (список маршрутов — из `app.routes`,
  4 измеримых дефекта дают ненулевой код выхода). CLAUDE.md — правило
  в обе стороны (§7) + `ui_audit.py` в чек-листе деплоя (§6 шаг 5). См.
  `docs/RUN_20260916_wrap_default.md`.
  **Живой результат**: horizontal_scroll/off_viewport/clipped_cell — 0
  находок сайтово (1 регрессия найдена и исправлена в процессе — /shift,
  заголовок «% готовности»). `small_font` — 2604 находки, ПРЕДСУЩЕСТВУЮЩИЙ
  долг сайтовых компонентов (badge/code/nav-label/kpi-label/kpi-sub),
  не относится к переносу текста, не чинил — KNOWN_ISSUES.md §44.
- Добавлены два пропущенных в исходном xlsx раздела вкладки «ОПН (рсм)»
  — ОПн197/ОПн198 (`migrations/036_id_opn_rsm_197_198.sql`). См.
  `docs/RUN_20260916_opn_197_198.md`.
- «Обзор ИД» на `/dashboard` пересобран по ТЗ Якименко А.И. (ПТО) от
  15.09.2026. См. `docs/RUN_20260915_id_overview.md`.
- Деплой-дисциплина (15.09.2026): правило "git → контейнер", печать
  запущенного коммита в лог, `docs/PROD_SYNC_2026-09-15.md`.

## Следующее
1. Первичные документы РСК/ИЗМ (акт №4183-156, построчные реестры) — на
   Google Drive физически нет, запрошены у ССР/РСК напрямую.
2. КЗ-40/КЗ-41/КЗ-42/КЗ-43/КЗ-44 из `KNOWN_ISSUES.md` (см. ниже) — ждут
   решения координатора, не блокируют очередь.

## Требует решения координатора
- KNOWN_ISSUES §44 (новое, 16.09.2026): шрифт < 14px сайтово в `.badge`/
  `code`/`.nav-label`/`.hotkey-hint`/`.kpi-label`/`.kpi-sub` — 2604
  находки новым `ui_audit.py`, не создано этой задачей. Поднять до 14px
  сайтово или завести отдельный пол для служебных мелких надписей —
  решение не моё. **Пока не закрыто — `ui_audit.py` в чек-листе деплоя
  (CLAUDE.md §6.5) сравнивать со счётом ДО правки, не с нулём.**
- `/id-rsk-link`, `id_form_row.id=968`: `section_label='Опв1- УТ1'` —
  висящий дефис и строчная «п» в данных, не нормализовал молча.
- KNOWN_ISSUES §43: `reload_id_form_v2.py`/`reload_id_rows_v3.py`
  перезапишут ОПн197/ОПн198 при повторном запуске.
- KNOWN_ISSUES §42: два законных, но разных «остатка» на /dashboard и
  /id-folders; «Текущая КС-2» в трубе — кумулятивно, не по периоду.
- `/id-grafik` существует отдельно от `/id-progress`, недостижима из
  меню ИД (пауза координатора 11.09.2026).
- KNOWN_ISSUES §41: 23 записи `id_form_entry` без вида работ прячут
  разделы у «Электрики» на `/id-progress`.
- KNOWN_ISSUES §40: подпись недели на `/id-progress` — диапазон вместо
  общего правила «дата понедельника».
- `docs/PROD_SYNC_2026-09-15.md` п.5: `pdfplumber`/`openpyxl` в
  requirements.txt, не установлены в живом образе.

## Задеплоено
`b310ec0` в проде на 16.09.2026 (сверка контейнер/git — momент коммита;
поверх него, тем же вечером, ещё две мелкие правки — `/shift` заголовок
и донастройка `ui_audit.py` — задеплоены на контейнер, коммит будет на
один хэш впереди `b310ec0` сразу после этой записи).
`check_consistency.py` 68/68 без расхождений.
