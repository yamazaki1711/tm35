# Текущее состояние работ

Обновлено: 16.09.2026

## Сейчас в работе
Ничего не в работе — последняя задача (подсказка Ctrl+Enter по числу
форм, не по классу) закрыта и задеплоена, см. ниже.

## Последнее сделанное
- `hotkeys.js`: подсказка «Ctrl+Enter» показывалась по угадыванию
  (класс кнопки не `.btn-secondary`), не по условию самого хоткея — на
  `/id-rsk-link` это давало до ~100 одинаковых подсказок подряд
  (столько же POST-форм «Прикрепить»), а хоткей при этом не срабатывал
  ни на одну (фокус после поиска — в GET-форме фильтра, кандидатов
  несколько). Заменено на `allSaveForms().length === 1` — тот же
  критерий, что решает, по какой форме бьёт сам хоткей. Обход всех
  маршрутов до/после: `/today` 316→0, `/id-rsk-link?row_id=968` 101→0,
  `/id-entry` 2→0, `/id-folders` 1→0 (тот же класс дефекта, найден тем
  же обходом, не был назван явно), 11 страниц с одной формой не
  сдвинулись (подсказка была и осталась). Ни одна страница не
  показывает больше одной подсказки после правки. См.
  `docs/RUN_20260916_hotkey_hint.md`.
- Инвертирован умолчательный перенос текста в таблицах (`td, th` теперь
  `white-space: normal` по умолчанию, `.nowrap` — явный маркер
  атомарного значения), пройдены все 35 шаблонов с `<table>`,
  `/id-rsk-link` приведён к паттерну `rsk_registry.html`/
  `rsk_processing.html`, `tools/ui_audit.py` впервые реально
  задеплоен и прогнан (список маршрутов из `app.routes`, 4 измеримых
  дефекта дают ненулевой код выхода). CLAUDE.md — правило в обе
  стороны (§7) + `ui_audit.py` в чек-листе деплоя (§6.5). См.
  `docs/RUN_20260916_wrap_default.md`. Живой результат:
  horizontal_scroll/off_viewport/clipped_cell — 0 находок сайтово;
  `small_font` — 2604 находки, предсуществующий долг, не относится к
  переносу текста — KNOWN_ISSUES.md §44.
- Добавлены два пропущенных в исходном xlsx раздела вкладки «ОПН (рсм)»
  — ОПн197/ОПн198 (`migrations/036_id_opn_rsm_197_198.sql`). См.
  `docs/RUN_20260916_opn_197_198.md`.
- «Обзор ИД» на `/dashboard` пересобран по ТЗ Якименко А.И. (ПТО) от
  15.09.2026. См. `docs/RUN_20260915_id_overview.md`.

## Следующее
1. Первичные документы РСК/ИЗМ (акт №4183-156, построчные реестры) — на
   Google Drive физически нет, запрошены у ССР/РСК напрямую.
2. КЗ-40/КЗ-41/КЗ-42/КЗ-43/КЗ-44 из `KNOWN_ISSUES.md` (см. ниже) — ждут
   решения координатора, не блокируют очередь.

## Требует решения координатора
- KNOWN_ISSUES §44: шрифт < 14px сайтово в `.badge`/`code`/
  `.nav-label`/`.hotkey-hint`/`.kpi-label`/`.kpi-sub` — 2604 находки
  `ui_audit.py`, не создано ни одной из последних трёх задач. Поднять
  до 14px сайтово или завести отдельный пол для служебных мелких
  надписей — решение не моё. Пока не закрыто — `ui_audit.py` в
  чек-листе деплоя (CLAUDE.md §6.5) сравнивать со счётом ДО правки, не
  с нулём.
- `/id-rsk-link`, `id_form_row.id=968`: `section_label='Опв1- УТ1'` —
  висящий дефис и строчная «п» в данных, не нормализовал молча.
- KNOWN_ISSUES §43: `reload_id_form_v2.py`/`reload_id_rows_v3.py`
  перезапишут ОПн197/ОПн198 при повторном запуске.
- KNOWN_ISSUES §42: два законных, но разных «остатка» на /dashboard и
  /id-folders; «Текущая КС-2» в трубе — кумулятивно, не по периоду.
- `/id-grafik` существует отдельно от `/id-progress`, недостижима из
  меню ИД (пауза координатора 11.09.2026).
- KNOWN_ISSUES §41: 23 записи `id_form_entry` без вида работ прячут
  разделы у «Электрики» на `/id-progress`.
- KNOWN_ISSUES §40: подпись недели на `/id-progress` — диапазон вместо
  общего правила «дата понедельника».
- `docs/PROD_SYNC_2026-09-15.md` п.5: `pdfplumber`/`openpyxl` в
  requirements.txt, не установлены в живом образе.

## Задеплоено
`448cb4c` в проде на 16.09.2026, сверка контейнер/git — момент
коммита (`docker logs tm_backend | grep '[deploy]'` совпадает с
`git rev-parse HEAD`). `check_consistency.py` 68/68 без расхождений.
`ui_audit.py --no-shots`: horizontal_scroll/off_viewport/clipped_cell —
0; small_font — 2598 (в пределах дрейфа живых данных от базовой находки
§44, эта правка была чисто JS и вёрстки таблиц не касалась).
