# Текущее состояние работ

Обновлено: 17.09.2026

## Сейчас в работе
Ничего не в работе — последняя задача (слияние «Папки»+«Реестр папок»)
закрыта и задеплоена, см. ниже.

## Последнее сделанное
- «Папки» и «Реестр папок» — один и тот же `id_folder` под разными
  именами колонок, координатор нашёл кликом по обоим пунктам меню.
  Слито в одну страницу `/id-folders`: 8 колонок одним именем каждая
  (Номер папки · Дата формирования · Разделов · Оценка ПТО, ₽ ·
  Сметная стоимость, ₽ · Подписант реестра передачи · Дата передачи в
  СДО · Стадия), фильтр+сортировка+CSV переехали из реестра, две
  плитки-суммы переименованы под источник («Сумма оценок ПТО, ₽» /
  «Сметная стоимость подписанных, ₽»). `/id-folders/registry` — теперь
  301-редирект с сохранением `status`/`sort`, не 404. Труба на
  `/dashboard` и `/id-folders` репointнута на `/id-folders?status=…` —
  все пять стадий проверены вручную, каждая ведёт ровно на своё
  множество (19/1/0/0/0). Дубль подписи «На проверке» в фильтре
  (canonical `checking` vs слитое `checking_all`) разведён:
  `checking` → «Проверка» (то же слово, что в колонке «Стадия»),
  `checking_all` остался «На проверке» (как в трубе). Пункт меню
  «Реестр папок» убран. См. `docs/RUN_20260917_id_folders_merge.md`.
- `hotkeys.js`: подсказка «Ctrl+Enter» — по `allSaveForms().length===1`,
  не по классу кнопки. См. `docs/RUN_20260916_hotkey_hint.md`.
- Инвертирован умолчательный перенос текста в таблицах, пройдены все 35
  шаблонов, `tools/ui_audit.py` — реальный гейт в чек-листе деплоя. См.
  `docs/RUN_20260916_wrap_default.md`.
- Добавлены ОПн197/ОПн198 на вкладку «ОПН (рсм)»
  (`migrations/036_id_opn_rsm_197_198.sql`). См.
  `docs/RUN_20260916_opn_197_198.md`.
- «Обзор ИД» на `/dashboard` пересобран по ТЗ Якименко от 15.09.2026.
  См. `docs/RUN_20260915_id_overview.md`.

## Следующее
1. Первичные документы РСК/ИЗМ (акт №4183-156, построчные реестры) — на
   Google Drive физически нет, запрошены у ССР/РСК напрямую.
2. КЗ-40/КЗ-41/КЗ-42/КЗ-43/КЗ-44 из `KNOWN_ISSUES.md` (см. ниже) — ждут
   решения координатора, не блокируют очередь.

## Требует решения координатора
- KNOWN_ISSUES §44: шрифт < 14px сайтово в `.badge`/`code`/
  `.nav-label`/`.hotkey-hint`/`.kpi-label`/`.kpi-sub` — 2598 находок
  `ui_audit.py` на 17.09.2026 (в пределах дрейфа живых данных от базовой
  находки), не создано ни одной из последних четырёх задач. Поднять до
  14px сайтово или завести отдельный пол для служебных мелких надписей
  — решение не моё. Пока не закрыто — `ui_audit.py` в чек-листе деплоя
  (CLAUDE.md §6.5) сравнивать со счётом ДО правки, не с нулём.
- `/id-rsk-link`, `id_form_row.id=968`: `section_label='Опв1- УТ1'` —
  висящий дефис и строчная «п» в данных, не нормализовал молча.
- KNOWN_ISSUES §43: `reload_id_form_v2.py`/`reload_id_rows_v3.py`
  перезапишут ОПн197/ОПн198 при повторном запуске.
- KNOWN_ISSUES §42: два законных, но разных «остатка» на /dashboard и
  /id-folders; «Текущая КС-2» в трубе — кумулятивно, не по периоду.
- `/id-grafik` существует отдельно от `/id-progress`, недостижима из
  меню ИД (пауза координатора 11.09.2026).
- KNOWN_ISSUES §41: 23 записи `id_form_entry` без вида работ прячут
  разделы у «Электрики» на `/id-progress`.
- KNOWN_ISSUES §40: подпись недели на `/id-progress` — диапазон вместо
  общего правила «дата понедельника».
- `docs/PROD_SYNC_2026-09-15.md` п.5: `pdfplumber`/`openpyxl` в
  requirements.txt, не установлены в живом образе.

## Задеплоено
`382157f` в проде на 17.09.2026, сверка контейнер/git — момент коммита
(`docker logs tm_backend | grep '[deploy]'` совпадает с
`git rev-parse HEAD`). `check_consistency.py` 68/68 без расхождений.
`ui_audit.py --no-shots`: horizontal_scroll/off_viewport/clipped_cell —
0 везде (включая слитую страницу и её редирект); small_font — 2598, в
пределах дрейфа от базовой находки §44, не относится к этой задаче.
