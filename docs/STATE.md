# Текущее состояние работ

Обновлено: 25.09.2026

## Сейчас в работе
Очередь пуста — «Обзор РСК»: новый набор плиток, ИКС/Лаборатория
убраны из отделов (25.09.2026) задеплоено, см. ниже. Открытые пункты —
в «Требует решения координатора».

## Последнее сделанное

### «Обзор РСК»: новый набор плиток; ИКС/Лаборатория убраны из отделов (25.09.2026)
- **Отделы**: «ИКС» (уже неактивна) и «Лаборатория» (была активна) —
  `active=false` (миграция 044), строки не удалены. На момент миграции
  0 нарушений на любую из них — переносить в «без отдела» было нечего.
  Код не менялся — список отделов везде уже фильтровался по `active`.
- **Плитки** (было: Активных замечаний/Готовы к снятию/Предъявить
  ИД/Корректировка ПД-РД/Отклонено РСК/Устранено) → **Всего замечаний /
  Устранено (зелёным) / Строй-площадка / ПТО / Корректировка ПД/РД /
  Отклонено РСК**. Убранные поля остаются в
  `compute_rsk_dashboard_stats()`, не выводятся. Каждая плитка —
  ссылка на `/rsk` с фильтром, дающим то же число. Новый параметр
  реестра `status` — фильтрует по уже вычисленному `status_key`
  (`rsk_pseudo_status()`), не отдельная формула.
- **Найдено и исправлено попутно**: «Корректировка ПД/РД» считала на 1
  больше бейджа/фильтра (сырое SQL не исключало `rejected=true`,
  `rsk_pseudo_status()` проверяет «Отклонено» раньше по приоритету) —
  добавлено исключение, обновлена и старая прямая проверка в
  `check_consistency.py`.
- **Разбиение**: Устранено(16) + ПТО(94) + ДПР(27) + строй-площадка(15)
  + без отдела(0) = Всего(152).
- `check_consistency.py` — 89→95. Живая проверка: все шесть плиток
  кликом до реестра дают то же «Найдено»; форма/фильтр показывают
  только ПТО/ДПР/строй-площадка.
- См. `docs/RUN_20260925_rsk_overview_tiles_and_departments.md`,
  `KNOWN_ISSUES.md` §69, скриншот
  `docs/screenshots_20260925_rsk_overview_tiles/`.

### РСК: выравнивание переключателя «Ответственный», один отдел вместо нескольких, «Загрузка акта» восстановлена и переработана (25.09.2026)
- **Выравнивание**: `.form-box label { display:block; margin-top:1rem }`
  (глобальное) продолжало действовать поверх `.rsk-checkbox-row label`,
### РСК: выравнивание переключателя «Ответственный», один отдел вместо нескольких, «Загрузка акта» восстановлена и переработана (25.09.2026)
- **Выравнивание**: `.form-box label { display:block; margin-top:1rem }`
  (глобальное) продолжало действовать поверх `.rsk-checkbox-row label`,
  которое не переопределяло ни `display`, ни `margin` — первый элемент
  группы (`:first-child`) получал `margin-top:0`, остальные — `16px`.
  Исправлено явными `display:inline-flex; margin:0`. Разброс центра
  всех переключателей после правки — 0.0px (было 8px).
- **Один ответственный отдел** (координатор, тем же днём): миграция 043
  — `rsk_processing.responsible_id` (FK), бэкофилл по факту на момент
  накатки (все 152 строки уже имели ровно один отдел — данные
  изменились за сутки с 24.09.2026, когда было 46 с несколькими).
  Старая m2m `rsk_processing_responsible` не удалена, новые записи
  через неё больше не пишутся. Форма — радиокнопки вместо чекбоксов, с
  пунктом «не назначен». Все потребители (реестр/фильтр/«Обзор РСК» →
  «по ответственным»/CSV) переведены на `responsible_id`.
- **«Загрузка акта» восстановлена**: пункт меню убрал коммит `751edf4`
  (17.09.2026) явным решением координатора — маршрут `/rsk/import`
  всё это время оставался рабочим. Координатор сегодня явно вернул
  пункт, решение 16.09.2026 отменено, не в конфликте с ним.
- **Отождествление нарушений между актами переписано**: было —
  равенство печатного «№» из PDF (`rsk_violation.sys_no unique`),
  задокументированное 06.09.2026 как непроверенное на втором акте
  предположение. Стало — нормализованный текст + раздел
  (`difflib.SequenceMatcher`), три порога (0.97 точное сразу; 0.80+
  отрыв 0.08 уверенно; ниже — неоднозначно, решает человек в форме,
  никогда не сливается молча). `rsk_processing` и так была на
  `violation_id` (миграция 024) — не требовала правки, риск был в том,
  что именно становится `violation_id` при повторном импорте.
  Дубликат акта — жёсткий отказ вместо тихого upsert.
- **Живая проверка**: повторная загрузка реального уже импортированного
  акта — 0 новых/0 снятых/152 перенесено/0 неоднозначных, дубликат
  отклонён (предпросмотр и прямой POST), БД не изменилась. Синтетический
  акт в ROLLBACK-транзакции (3 нарушения убраны, 149 переномерованы,
  1 новая позиция) — диф в точности совпал с ожиданием,
  `rsk_processing` перенесённых нарушений побайтово идентична до/после.
  Неоднозначность отдельно проверена юнит-тестом на подменённом пуле
  кандидатов. `check_consistency.py` — 85→89.
- См. `docs/RUN_20260925_rsk_checkbox_single_responsible_act_import.md`,
  `KNOWN_ISSUES.md` §68, скриншоты
  `docs/screenshots_20260925_rsk_single_responsible/`,
  `docs/screenshots_20260925_rsk_import_restore/`.

### /dashboard, «Обзор ИД»: две плитки перекрашены в зелёный — «Подписано по КС-3» и «Подписано» (разделы), 25.09.2026
- Чисто визуальная правка: класс `kpi-num ok` добавлен к «Подписано по
  КС-3 с НДС, ₽» и к «Подписано» (ряд «Всего разделов»), тот же класс,
  что уже красит «1975 Подписано» в ряду «АОСР (видов работ)». Числа и
  SQL не менялись.
- Та же пара плиток дублируется на `/id-folders` («Деньги»/«Разделы») —
  правка внесена и там, чтобы метрика выглядела одинаково на обеих
  страницах.
- Проверено вычисленным цветом (`getComputedStyle`): обе плитки —
  `rgb(18, 153, 106)`, точное совпадение с эталоном. Остальные плитки
  не изменились. `check_consistency.py` 88/88 (без изменений).
- См. `KNOWN_ISSUES.md` §67, скриншоты
  `docs/screenshots_20260925_dashboard_green_tiles/`.

### Follow-up к РСК «Снятые» (24.09.2026): шесть плиток «Обзора РСК» и «по ответственным» переведены на rsk_violation_is_removed()
- Координатор ответил на оба открытых пункта `KNOWN_ISSUES.md` §66:
  (1) шесть плиток и разрез «по ответственным» ДОЛЖНЫ считать «снято»
  тем же критерием, что и реестр — предыдущий заход ошибочно оставил их
  на голом `is_active`; (2) `/rsk/processing` и `id_row_rsk_link`
  СОХРАНЯЮТ показ уже «Снятых» нарушений (легитимно — снять отметку по
  ошибке, привязать снятое нарушение к разделу ИД), добавить только
  статус-бейдж.
- `compute_rsk_dashboard_stats()` — все шесть тайлов и `by_responsible`/
  `no_responsible` теперь на `not (RSK_VIOLATION_REMOVED_SQL)` вместо
  `v.is_active`. Числа: Активных замечаний 152→136, Готовы к снятию
  18→2, Предъявить ИД 49→45, Корректировка ПД/РД 35→35 (без изменений),
  Отклонено РСК 4→4 (без изменений), Устранено 16→16 (число то же,
  смысл — теперь прямой дубль «снято» целиком, а не «resolved среди
  активных», иначе было бы тождественно 0 при новом пуле).
- `/rsk/processing` — фильтр списка НЕ тронут (уже показывал верный
  бейдж через `rsk_pseudo_status()` с прошлого захода). `id_rsk_link.-
  html`/`compute_id_row_rsk_link_data()` — добавлен столбец «Состояние»
  (бейдж «Снято»/«Активно») в обеих таблицах, фильтр кандидатов не
  менялся.
- `check_consistency.py` — 85→88 (тайл vs `/rsk?state=active`, тайл vs
  `/rsk?state=closed`, сумма «по ответственным» = тайл «Активных
  замечаний» через `count(distinct violation_id)` — сырая сумма
  `by_responsible[].n` законно больше total_active, 46 нарушений имеют
  больше одного ответственного).
- См. `docs/RUN_20260924_rsk_overview_unify.md`, `KNOWN_ISSUES.md` §66,
  скриншоты `docs/screenshots_20260924_rsk_overview_unify/`.

### ТЗ №11 Якименко А.И. (24.09.2026): плитки «Прогресс по видам работ» на /dashboard вместо «Потока по вкладкам»; карточка папки упрощена
- **Блок А**: `/dashboard` («Обзор ИД») больше не показывает «Поток по
  вкладкам» (ТЗ №10) — вместо него плитки «Прогресс по видам работ»,
  тот же партиал (`_id_progress_tiles.html`) и та же функция
  (`compute_id_progress_tiles()`), что и на `/id-progress`. Состав
  плиток: «АОСР (видов работ)» (было «Связок раздел+вид работы с
  записью», логика та же), «Разделов хотя бы с одной записью» убрана
  целиком, «Подписано»/«Подписано в карандаше» без изменений, «В цикле
  РСК» → «На проверке в РСК», «Стопперы» → «Не приступали» (новая
  формула: тайл1 − тайл3 − тайл4 − тайл5, арифметика, не отдельный SQL
  — тождество верно по построению). Разбор «Не приступали» по кодам
  статуса (872/107/52/19 = 1050) — в run log, не на экране.
- **Блок Б**: карточка папки (`/id-folders/{id}`) — «Передача в СДО» и
  «Проверка» убраны из UI (данные в БД остаются, читаются как раньше
  везде, где уже были прочитаны); «Подписание» и «КС-2» слиты в одну
  форму «Подписание и КС-2» — один эндпоинт пишет `signed_date=ks2_date`
  одной датой, больше не может разойтись для новых/изменённых записей;
  «Стоимость после подписания» убрана — 0 из 21 папки её использовали
  (проверено), «Стоимость при сборке» и так была единственным реально
  работающим входом, переносить было нечего.
- **17 из 21 папки** имеют исторически расходящиеся `signed_date`/
  `ks2_date` (заведены до слияния) — БД НЕ менялась, UI показывает
  `ks2_date`, когда она есть, иначе `signed_date` (решение координатора).
  Полный список папок — `docs/RUN_20260924_tz11_yakimenko.md`, §Б3.
- Живая проверка на throwaway-папке: создание → подписание+КС-2 →
  правка стоимости на уже подписанной папке → удаление, тайлы «Подписано
  ИД»/«Подписано по КС-3» пересчитывались на каждом шаге и вернулись
  ровно к исходным числам после отката.
- См. `docs/RUN_20260924_tz11_yakimenko.md`, `KNOWN_ISSUES.md` §65,
  скриншоты в `docs/screenshots_20260924_tz11/`.

### Поправка к ТЗ №10 (24.09.2026): /gantt — весь директивный период вместо точки-дефолта; VAT-вопрос закрыт координатором; РСК «Снятые» — подтверждено без изменений
- **/gantt, дефолт окна — переделан, не восстановлен**: заход
  23.09.2026 заменил дефолт «начало окна отображения» (01.08.2026) на
  «сегодня−7» и прямо назвал это отменой решения координатора от
  01.09.2026 «по указанию задания» — **неверно**, задание говорило
  «исправить первопричину», это не то же самое, что разрешение
  переопределить документированное решение. Формулировка снята
  (`docs/RUN_20260924_tz10_yakimenko.md`, правка в §«Пункт 6b»).
  Настоящее исправление: по умолчанию `/gantt` показывает **весь
  директивный период целиком** (`get_directive_start()`/
  `get_directive_deadline()` — существующие настройки, не хардкод),
  автоматически расширенный, если у какой-то работы плановый срок
  (`current_schedule.current_start`/`current_finish`) выходит за эти
  границы, и всегда включающий сегодняшнюю дату (новая функция
  `_gantt_default_range()`, `main.py`). Расширение — по срокам работ,
  НЕ по `daily_progress.date` (там есть записи с 18.06.2026 — решение
  координатора от 29.08.2026 прямо не показывает их здесь, включать их
  в диапазон значило бы тихо отменить и это решение тоже). Баннер
  `#gantt-today-note` убран — с гарантированным включением «сегодня» в
  диапазон он больше не может сработать. Живая проверка: тестовые
  работы на 25.08.2026 и 05.12.2026 (обе — вне 01.08–28.11) обе видны
  сразу после сохранения, диапазон расширился корректно; убраны после
  проверки.
- **VAT-вопрос закрыт координатором, не открытый вопрос больше**:
  `id_folder.amount_rub` не имеет фиксированного налогового базиса —
  заполняется вручную по каждой папке, редактируется в любой момент;
  прежняя запись «без НДС» описывала терминологию одной прошлой задачи
  (экспорт «График ИД», `docs/decisions_needed_grafik_id_export.md`
  §16), не постоянное свойство колонки. Докстринг `id_folder_cost()`,
  `docs/decisions_needed_grafik_id_export.md` §16 и `KNOWN_ISSUES.md`
  §63 обновлены. Значения в БД не менялись.
- **РСК «Снятые» — подтверждено, без изменений кода**: пункт уже
  включён и выбираем при нулевом счёте (не задизаблен), таблица при
  нулевом результате уже показывает обычное пустое состояние без
  лишнего текста — оба требования проверки уже выполнялись до этого
  захода. В БД по-прежнему один акт (№4183-159, 11.08.2026), 0 снятых.
  **ПОПРАВКА — этот вывод был неверным, см. следующую запись ниже
  («РСК «Снятые»: определение исправлено») и `KNOWN_ISSUES.md` §66.**
- См. `docs/RUN_20260924_tz10_followup.md`, `KNOWN_ISSUES.md` §63.

### РСК «Снятые»: определение исправлено — структурно ИЛИ «Устранено» (3-е обращение координатора, 24.09.2026)
- Причина 3-го обращения: прошлый заход доказал МЕХАНИЗМ фильтра
  рабочим и честно объяснил «Найдено: 0» одним актом — но принял
  структурное закрытие (`is_active=false`) за ЕДИНСТВЕННЫЙ критерий
  «снято», не заметив второй, независимый признак реестра —
  «Устранено» (`rsk_processing.resolved`+`resolved_date`, слой 2 формы
  `/rsk/processing`). Определение было неполным, не сам механизм.
- **Решение координатора**: снято = (а) структурно закрыто (как было)
  ИЛИ (б) «Устранено» = «да»/дата. Слои остаются раздельными в модели
  данных — объединяет их только определение фильтра/статуса.
- Единая функция `rsk_violation_is_removed()`/`RSK_VIOLATION_REMOVED_SQL`
  (`main.py`) — используется в фильтре «Состояние» `/rsk`, бейдже
  `rsk_pseudo_status()` и CSV-экспорте (через бейдж; отдельно исправлен
  формат «Устранено» в CSV — было пусто вместо «да» для 14 строк).
  Сознательно НЕ применена к шести плиткам «Обзора РСК», разрезу «по
  ответственным», списку `/rsk/processing`, поиску `id_row_rsk_link` —
  все четыре остаются на `is_active` (обоснование и вынесенные решения
  координатору — `KNOWN_ISSUES.md` §66).
- Данные (24.09.2026): 136 не устранено, 14 «да», 2 с датой; структурно
  закрыто 0; пересечение 0. Итог: Снятые=16, Активные=136, 16+136=152
  (весь реестр, без пропуска/пересечения). 5 точечных `sys_no`
  проверены живьём после деплоя — присутствуют ровно в «Снятые»,
  отсутствуют в «Активные», бейдж согласован. `check_consistency.py`
  — 3 новые проверки (85/85).
- Данные не менялись — только логика чтения/фильтрации.
- См. `docs/RUN_20260924_rsk_removed_definition_fix.md` (полный разбор:
  источник поля, профиль значений, все потребители с обоснованием,
  скриншоты), правка в `docs/RUN_20260924_tz10_followup.md` §Пункт 3,
  `KNOWN_ISSUES.md` §66.

### ТЗ №10 Якименко А.И. (23.09.2026): «с НДС» на деньгах, Обзор ИД (труба→поток, тайлы, КС-2→ИД), новый сектор КС-3, РСК «Снятые» (честный ноль), фикс дефолта /gantt — см. поправку выше
- **«с НДС»** добавлено ко всем денежным подписям сайта (тайлы, колонки,
  формы, CSV, флеш-сообщения) — только подписи, ни одно значение не
  пересчитывалось. Источник НДС проверен по каждому полю — VAT-вопрос
  про `id_folder.amount_rub` закрыт координатором 24.09.2026 (см. выше),
  не открытый вопрос.
- **Обзор ИД**: труба «Выполнение» убрана с `/dashboard`, заменена
  партиалом `_id_progress_stream.html` (вынесен из `id_progress.html`,
  та же `compute_id_progress_stream()` на обеих страницах — 15
  построчных сверок в `check_consistency.py`, тайл-в-тайл). Тайлы:
  убрано «Подписано ранее», добавлено «Подписано по КС-3» (новый
  сектор), «Подписано по КС-2» → «Подписано ИД» (то же значение).
  Формула «Остаток по контракту» — контракт − КС-3 − невыбираемый
  остаток (КС-2/ИД больше не вычитается — не считать одни деньги дважды).
- **Новый сектор «КС-3»** на `/id-folders` (миграция 042,
  `id_ks3_entry`) — тот же паттерн, что «Невыбираемый остаток», плюс
  редактирование (`<details>`, без JS). Живая проверка: 4 тестовые
  записи из примера задания дали ровно **98 564 872,66** (дословно
  число из задания), правка и удаление пересчитывают тайл корректно,
  тестовые данные не остались в проде.
- **Стоимость папки (п.5b)** — проверено: уже редактируется в любом
  состоянии папки, зависимые тайлы пересчитываются немедленно (живой
  тест на ТМ-001: 3 285 192,73 → 4 000 000,00 → тайл +714 807,27 →
  возвращено обратно). Правка не потребовалась.
- **РСК «Снятые» (п.6a)** — механизм доказан рабочим живым скриптом
  внутри откаченной транзакции (симуляция закрытия 3 реальных нарушений
  «вторым актом», фильтр вернул ровно их). Честный «Найдено: 0» на
  экране — в БД всего один акт, закрывать не с чем, не дефект.
- **СМР Гант (п.6b)** — воспроизведено end-to-end (добавление работы,
  смена сроков, ввод факта через `/shift`): данные пишутся и читаются
  верно и немедленно везде. Единственная причина «не обновляется» —
  `/gantt` открывался с 01.08.2026, а не с текущей даты; разрыв к
  23.09.2026 вырос до полутора месяцев. **Этот заход менял дефолт на
  «сегодня−7» и ошибочно называл это отменой решения координатора «по
  указанию задания» — исправлено 24.09.2026, см. поправку выше:
  правильное решение — показывать весь директивный период, не менять
  одну точку-дефолт на другую.**
- См. `docs/RUN_20260924_tz10_yakimenko.md`, `KNOWN_ISSUES.md` §63,
  скриншоты в `docs/screenshots_20260924_tz10/`.

### /id-progress: статус-фильтр рядом с «Вкладка» на матрице «Раздел × Этап»; премиса задания о «лишнем статусе Труба Сварки» оказалась устаревшей на 18 дней (задание координатора 21.09.2026)
- **Второй `<select name="status">`** в той же форме, что «Вкладка»,
  список — живой запрос `id_form_status` для ВЫБРАННОЙ вкладки, `order
  by display_order`; `Все` первым/по умолчанию, `Без записи о статусе`
  последним.
- **Фильтр строк матрицы** — не новое правило: та же величина, что уже
  красит замороженный столбец (`current_color`) — последняя отправленная
  запись `id_form_entry` по разделу, любой вид работ (принцип
  `LATEST_ID_FORM_ENTRY_CTE`, НЕ по-worktype-версия). Ячейки внутри
  отфильтрованной строки не меняются.
- **Находка (`KNOWN_ISSUES.md` §62)**: задание утверждало «9 статусов
  на большинство вкладок + 10-й только на Труба Сварка» и просило это
  продемонстрировать — проверено живым запросом по всем 17 вкладкам:
  **список сейчас идентичен на всех** (10 статусов). Причина — это уже
  закрытый вопрос (§14: координатор унифицировал справочник 03.09.2026,
  статус действительно был только на 3 вкладках ДО этого решения, удалён
  по прямому указанию; миграция 031 добавила «Подписано в карандаше»
  сразу всем). Задание процитировало состояние двухнедельной давности.
  Возвращать статус ради демо значило бы отменить принятое решение без
  нового указания — не стал (правило «отрицательный результат
  окончателен»). Показано вместо этого: дропдаун читает СВОЙ набор строк
  на каждую вкладку (разные id у одинаковых по тексту статусов) — не
  захардкожен, при том что содержимое сейчас намеренно одинаково.
- **Окно недель переживает обе смены** (`week_offset`/`show_all` —
  скрытые поля в форме + добавлены во все навигационные ссылки матрицы,
  которых раньше не несли). Смена вкладки сбрасывает статус на «Все» —
  id статуса чужой вкладки ничего не значит (своя копия строк на каждую
  вкладку).
- **Живая сверка для одной вкладки** (Труба Сварка): по статусам —
  2+25+3+0+0+0+0+21+0+0 + 17 (без записи) = **68 = 68** невфильтрованных
  разделов.
- **Легенда «3/7 — подписано видов работ / всего видов работ»**
  проверена — статический текст без переменной (объясняет формат ячейки,
  не агрегат по вкладке), фильтр её не задевает по построению, править
  не стал.
- См. `docs/RUN_20260921_id_progress_status_filter.md`.

### /id-progress: легенда «Поток по вкладкам»; найдено и задокументировано расхождение цветов с легендой матрицы (задание координатора 21.09.2026)
- **Семантика подтверждена живым запросом, не предположением**: зелёный
  — `Подписано`; жёлтый — весь цикл РСК (4 статуса) + `Подписано в
  карандаше`; красный — всё остальное. Связки без единой записи о
  статусе (**222** штуки, вне вкладок opv/n) не попадают ни в один из
  трёх фильтров (`NULL`-семантика SQL, подтверждено и вживую) — легенда
  говорит об этом отдельной фразой.
- **Легенда добавлена** тем же компонентом, что у матрицы
  (`.id-matrix-legend`), новый CSS не вводился — сразу под блоком
  «Поток по вкладкам», перед `<h3>Матрица «Раздел × Этап»</h3>`.
- **Находка (`KNOWN_ISSUES.md` §61)**: легенда стрима и легенда матрицы
  используют одинаковые цвета для разных множеств статусов — совпадает
  только зелёный. Жёлтый в стриме — весь цикл РСК, в матрице — только
  первый его шаг; красный в стриме — «всё остальное» по исключению, в
  матрице — конкретная причина блокера; синий есть только у матрицы.
  Обе легенды однозначны каждая сама по себе. Рекомендация — выравнивать
  стрим к матрице (богаче различает причину блокировки), не наоборот;
  решение — координатора, цвета матрицы в этом задании не менялись.
- **Новая проверка `check_consistency.py`**: сумма
  зелёный+жёлтый+красный по всем вкладкам стрима против плитки
  `total_pairs` — совпадают, **2872 = 2872**.
- См. `docs/RUN_20260921_id_progress_legend.md`.

### /rsk/processing: клик по пустому месту в «Ответственный» больше не ставит «ПТО», форма перегруппирована по смыслу (задание координатора 21.09.2026)
- **Баг**: `<label style="display:block">Ответственный <div>…</div></label>`
  оборачивал целую группу чекбоксов — implicit label-binding уходит к
  ПЕРВОМУ labelable-потомку («ПТО»), не к смыслу подписи; клик по
  пустому месту правее «Лаборатория» отмечал «ПТО». Исправлено
  структурно: `<fieldset>`+`<legend>` вместо оборачивающего `<label>`
  (legend физически не привязывается ни к чему). Тот же класс дефекта
  найден и исправлен на `/rsk/import` («Файл акта (PDF)», вложенный
  `<label for=...>` внутри внешнего `<label>`).
- **Перегруппировка**: восемь полей формы, раньше в одном `rsk-form-
  grid` (auto-fit раскидывал «Устранено» и «Дата снятия» по разным
  строкам), сгруппированы по смыслу — три трека одной строкой,
  условие+плановая дата снятия вместе, устранено+дата снятия рядом,
  отклонено РСК отдельно, ответственный — тот же fieldset с чекбоксами
  в один ряд (ТЗ 16.09.2026, сохранено).
- **Живая проверка**: 4 клика по пустому месту в блоке «Ответственный»
  (включая реальную мышь, не только синтетическое событие) — состояние
  чекбоксов не изменилось; клик по самому чекбоксу по-прежнему работает.
  `/rsk/import` проверен полным циклом: загрузка реального файла →
  предпросмотр (152 позиции, 0 проблем разбора) → тот же результат, что
  и раньше, парсер не задет правкой.
- **Новая проверка `tools/ui_audit.py`**: `label_scope` (`<label>` с
  более чем одним labelable-потомком или вложенным `<label>` —
  провальная) — **2 → 0** хита сайтово.
- См. `docs/RUN_20260921_rsk_processing_label_fix.md`, `KNOWN_ISSUES.md`
  §60.

### Таблицы влезают в свою рамку — паддинг ячейки сайтово, два новых правила в ui_audit.py (задание координатора 21.09.2026)
- **Диагноз подтверждён измерением, не на глаз**: на `/rsk` десять
  колонок, каждая уже без запаса (ни одна не over-allocated — измерено
  живьём), суммарно были на 81px шире рамки на 1366×768 — не «Физика
  занимает вдвое больше», как показалось координатору, а паддинг
  `.85rem` с обеих сторон × 10 колонок.
- **Правка сайтовая**: горизонтальный паддинг `th, td` снижен с `.85rem`
  до `.5rem` (вертикальный не менялся) — общее правило `style.css`, не
  точечное под `/rsk`. `/rsk` после правки: `scrollWidth === clientWidth`,
  overflow 0.
- **Обход всех таблиц сайта** нашёл ещё три места того же дефекта:
  `/changes` (закрылось паддингом), `/id-entry` (колонка «Блокировка»
  была `.nowrap` на предложении — снят nowrap), `/id-folders/bulk-entry`
  (заголовки-фразы были `.nowrap` — сняты, данные-значения остались
  атомарными и nowrap). `/gantt`, `/shift`, `/id-progress` — уже
  задокументированное исключение CLAUDE.md §7 (сетка растёт с числом
  дней, прокручивается сама) — не трогал.
- **Найдена, но не в списке CLAUDE.md**: `/id-grafik/category-mapping`
  — та же форма (колонка на категорию), не выделена как исключение —
  оставлена информационной в новой проверке, требует решения
  координатора (формализовать или перепроектировать).
- **Два новых правила в `tools/ui_audit.py`**: `table_overflow`
  (таблица шире `.table-wrap` — провальная везде, кроме
  задокументированных исключений) — **9 → 0** провальных находок;
  `column_over_allocated` (колонка шире содержимого+паддинга —
  информационная, на `/rsk` — 0 и до, и после, подтверждает диагноз).
- См. `docs/RUN_20260921_tables_fit_frame.md`, `KNOWN_ISSUES.md` §59.

### РСК: колонка «Снято» убрана из реестра и CSV — дублировала СТАТУС (задание координатора 20.09.2026)
- Перед правкой — сверка §6 шаг 1: все файлы контейнера (`main.py`,
  `style.css`, все 44 шаблона, 15 из 17 `tools/*.py`) побайтово совпали
  с `origin/main`; два отсутствующих в контейнере скрипта
  (`cycle1_audit.py`, `patch_xlsx_template_poc.py`) — справочные файлы,
  не вызываются приложением, не деплоятся никогда. Деплой отставал от
  git на один docs-коммит (без кода) — ожидаемо, не расхождение.
- `/rsk`: столбец «Снято» (`closed_act_date`) убран — нарушение,
  отсутствующее в последнем акте, уже показывает бейдж «Снято» в
  столбце СТАТУС, вторая колонка дублировала то же самое. `/export/-
  rsk.csv`: убрана та же колонка (ничего постороннего её не читало,
  проверено grep) — экран и выгрузка снова совпадают по составу.
  `closed_act_date` остался в `RSK_LIST_SELECT_SQL` (не читается больше
  нигде, оставлен как есть — не трогал JOIN/модель закрытия). Фильтр
  «Состояние → Снятые» читает `v.is_active` напрямую, не этот столбец —
  не затронут. «Устранено» не менялась.
- `/quality`, проверка «Отмечено «Устранено», но нарушение всё ещё в
  последнем акте» — **15 до, 15 после** (не читает разметку реестра,
  независимый SQL по `rsk_processing`/`rsk_act_item`).
- См. `docs/RUN_20260921_rsk_removed_snyato_column.md`.

### Убрана выдуманная запись из прода, устранён перенос строки плиток, аудит ТЗ пересобран по дословному тексту (задание координатора 19.09.2026)
- **Срочное: тестовая запись, оставленная «как реальная» 18.09.2026,
  убрана из прода.** Работа `TM35-MAIN-043` (id=143) — удалена
  фабрикованная строка `daily_progress` (`id=62516`), `work.fact_pct`
  восстановлен в 0, `current_schedule` для неё удалена. Подтверждено на
  экране: `/gantt` — 0%/«не начата»/без графика, `/dashboard` и
  `/status` — 28,8% (было 29,0% с фабрикацией). Проверены все 11 run
  log с 15.09.2026 на предмет других тестовых записей — найдено ещё
  четыре (RSK №387, предпросмотр импорта акта, работа `TM35-AUX-005`,
  тестовый стоп-фактор «погода») — все подтверждены откаченными заново,
  напрямую в БД, не со слов старых run log. Реальная активность ПТО
  (`smetanenko`, 17-18.09.2026, `TM35-MAIN-110`/`111`) — не трогалась,
  это не тест. Правило добавлено в `CLAUDE.md` §7 «Данные»: тестовая
  запись откатывается в том же шаге со сверкой до/после, иначе прод не
  оставляют «как проверено».
- **Строка плиток (`.kpi-row`) больше не переносится с «хвостом».**
  `grid-template-columns: repeat(auto-fit, minmax(200px,1fr))` считал,
  сколько колонок помещается по 200px, не сколько плиток в ряду — 6
  плиток «Обзора РСК» уезжали 5+1 на `/` и `/dashboard`. Заменено на
  `flex` с `flex:1` на каждой плитке — всегда одна строка, ширина блока
  делится строго на N. Новая находка в `tools/ui_audit.py`
  (`kpi_row_wrap`, сравнение `offsetTop` потомков `.kpi-row`) —
  **6 → 0**. Правило добавлено в `CLAUDE.md` §7 «Интерфейс».
- **Аудит ТЗ Якименко 16.09.2026 пересобран по дословному тексту** —
  `docs/TZ_Yakimenko_16092026_verbatim.md` (54 строки) добавлен в
  репозиторий, заменяя пересказ, использованный 18.09.2026. Из 54
  строк: 10 — заголовки/описания старого состояния, 40 — done, 4 —
  deliberately deviated (23/25/26 — внутреннее противоречие ТЗ или
  прямой запрет трогать формулы трубы, у координатора; 53/54 — стоят по
  отдельным уточнениям координатора поверх буквального текста). Ни
  одной строки «not done» не нашлось — прежний аудит по пересказу не
  разошёлся с оригиналом по существу.
- См. `docs/RUN_20260919_fabricated_fact_removed_kpi_wrap_fixed_tz_-
  audit_rebuilt.md`.

### «Качество данных» перестала быть архивом; инвентаризация свежести данных по всем страницам (задание координатора 18.09.2026)
- **Подтверждено с числами**: `work.data_quality_flag`/`daily_progress.-
  data_quality_flag`/`import_unresolved_cell` пишутся только офлайн-
  скриптом импорта Excel (снят с 01.09.2026), ничем не снимаются — 7
  строк `work` с меткой (все — прогон 29.08.2026), 0 строк
  `daily_progress`, `import_unresolved_cell` пуста (0 строк).
- **`/quality` перестроена**: два архивных раздела заменены двенадцатью
  живыми проверками СМР/ИД/РСК (`compute_quality_checks()` в
  `main.py`) — вне директивного периода (835, из них 216 через
  веб-форму), факт превышает план на 3+ человека (10), % выполнения
  падает день-к-дню (1), факт после 100% (1), нет плановых сроков (25),
  разделы ИД без вида работ (24), нарушения РСК «Устранено», но ещё в
  последнем акте (15), и ещё пять с честным нулём. Каждая находка —
  ссылка на запись. Архив нераспознанных ячеек — вниз, честно датирован
  (сейчас: «дата не восстановима, таблица пуста»). Найдена и исправлена
  по пути регрессия рендера (835 строк без предела вешали браузер) —
  добавлен предел показа 150, счётчики остались полными.
- **Инвентаризация `docs/DATA_FRESHNESS_INVENTORY_2026-09-18.md`** — 45
  страниц, только чтение. Две находки вне `/quality`: тайл «Всего по
  контракту, ₽» читается из хардкод-константы в Python, не из таблицы
  `contract` (существует, не используется приложением); расчёты
  трудоёмкости (`/calculator`, `/status`, `/dashboard`, `/today`,
  `/critical`) живые механизмом, но считают по срезу `daily_progress`
  (`source='excel_import'`), не пополнявшемуся с 29.08.2026.
- См. `docs/RUN_20260918_quality_page_and_data_freshness.md`,
  `docs/DATA_FRESHNESS_INVENTORY_2026-09-18.md`, `KNOWN_ISSUES.md` §58.

### Довели ТЗ Якименко А.И. (16.09.2026) до конца: форма «Ввод факта» на странице, четыре хвоста ТЗ, построчный аудит (задание координатора 18.09.2026)
- **Модалка «Ввод факта» (задача 3, 17.09.2026) заменена формой прямо на
  `/shift`** — координатор указал, что модалка была неверным прочтением
  исходного ТЗ («форма на экране, не диалог по клику»). `#modal-fact` и
  весь её JS удалены целиком, поля перенесены в обычный `.form-box` над
  таблицей дня, markup — по образцу восстановленного `form.html`
  (задача 3 его удалила; шаблон и маршрут `/form` остаются мёртвым
  редиректом, как и раньше — сам файл используется только как образец
  разметки, не маршрутом). Подписи подняты до 16px точечным селектором,
  не трогая сайтовый `.85rem`-долг (`KNOWN_ISSUES.md` §44).
- **Найдена и исправлена собственная регрессия того же деплоя**:
  `render()` безусловно вызывал функцию, существующую только когда
  форма отрендерена (право `smr:write`) — для доступа только на чтение
  весь `/shift` падал целиком (`TypeError`), не только форма. Поймано
  собственным прогоном `ui_audit.py` сразу после первого деплоя
  (числа выглядели подозрительно хорошо), исправлено и передеплоено в
  течение нескольких минут — прод не оставался в сломанном состоянии
  дольше короткого окна между двумя деплоями.
- **Четыре хвоста ТЗ 16.09.2026 закрыты**: `/id-folders` — заголовок
  воронки «Выполнение» (было не унифицировано с `/dashboard`), заголовок
  формы ручного объёма «Невыбираемый остаток» (было расхождение с
  соседней плиткой), дропдаун «Статус» получил пункт «Все» первым/по
  умолчанию (`KNOWN_ISSUES.md` §46, закрыто); значок статуса РСК
  `needs_rd` переименован в «Корректировка ПД/РД» вслед за тайлом
  (`KNOWN_ISSUES.md` §49, закрыто). Repo-wide grep по десяти терминам
  задания — только исторические докстринги/комментарии, ничего живого
  не осталось несделанным.
- **Построчный аудит всего ТЗ 16.09.2026** — таблица по всем трём
  прошлым задачам (ИД/РСК/СМР) в `docs/RUN_20260918_shift_form_and_tz_-
  audit.md`, с оговоркой: ТЗ оказалось процитировано НЕ буквально ни в
  одном из трёх run log 17.09.2026 (вопреки утверждению задания) —
  таблица построена по пересказу, не по цитате; если у координатора
  сохранился исходный файл ТЗ — стоит перепроверить.
- **Живой тест до/после**: работа `TM35-MAIN-043` через новую форму —
  `0%`/«не начата»/без графика → `15%`/«в работе»/график 20–25.09.2026,
  общий взвешенный прогресс `28,8%` → `29,0%` на `/dashboard` и
  `/status` одинаково. Запись НЕ откатывалась (реальный факт, не тест).
- `tools/ui_audit.py`: `small_font` **2676 → 2667** (−9, по всему сайту)
  — единственная причина: один `<option>` внутри удалённой модалки,
  который вообще засчитывался детектором (на трёх маршрутах-редиректах
  на одну страницу × 3 разрешения). `check_consistency.py` — 81/81 без
  расхождений, число проверок не изменилось.
- См. `docs/RUN_20260918_shift_form_and_tz_audit.md`,
  `KNOWN_ISSUES.md` §57 (новое).

### Расследование — сколько данных стоил `daily_progress_audit_trap_trg` (задание координатора 18.09.2026)
- **Только чтение `daily_progress`** — задание прямо это требовало, ни
  одна строка факта не менялась.
- Точная хронология по логу `kat_postgres` (ретеншн с 2026-07-11, без
  разрывов): механизм работал нормально с 31.08.2026, сломан
  15.09.2026 23:21:01 UTC (уборочная сессия унесла рабочую таблицу в
  схему `archive` вместе с настоящими бэкапами, по совпадению имени) —
  **не** предшествует переходу на 01.09 без Excel, вопреки худшему
  сценарию задания.
- **29 неудачных попыток правки, 18 уникальных пар (дата, работа)**,
  двумя окнами: 25 попыток 16.09 (2 ч 23 мин, основной инцидент), ещё
  4 попытки 17.09 (20 мин — первое исправление сессии 17.09,
  `040_daily_progress_audit_trap_table.sql`, само оказалось неполным:
  новая таблица досталась в собственность `kat_admin`, роль приложения
  `tm35_app` доступа не получила). Полный реестр по дням —
  `docs/DAILY_PROGRESS_LOST_EDITS_2026-09-18.md`.
- **Настоящий фикс проще, чем казалось**: оригинальная таблица (2301
  строка истории по 15.09.2026) всё время была цела в `archive` —
  возвращена в `public` миграцией `041_fix_daily_progress_audit_trap_-
  ownership.sql`, дубликат от 040 удалён. Проверено под ролью
  приложения, не администратора.
- Во всей БД (все схемы) — ровно одна пользовательская функция и один
  триггер, оба уже дважды перепроверены; других объектов с тем же
  классом риска нет.
- `tools/check_consistency.py` — два новых постоянных пункта: статический
  разбор триггерных функций на ссылки к несуществующим таблицам, живой
  `UPDATE`+`ROLLBACK` на `daily_progress`. 81/81 без расхождений.
- См. `docs/RUN_20260918_daily_progress_audit_assessment.md`,
  `KNOWN_ISSUES.md` §53 (полностью переписан с итоговой оценкой).

### Задача 3 — СМР (ТЗ Якименко А.И., 16.09.2026, с уточнением координатора 17.09.2026)
- **Ввод факта переехал на `/shift`**: `/form`/`/baseline` убраны из
  меню и редиректят на `/shift`. Кнопка «+ добавить работу» заменена
  модалкой «Ввод факта» (те же поля, что были на `/form`, плюс сроки
  графика и подрядчик) — создание новой работы осталось на `/gantt`.
- **Найдена и устранена причина «график не обновляется»**: три
  несвязанных хранилища планового срока (`baseline_schedule` ←
  `/baseline`; `current_schedule` ← только `/gantt`;
  `work.plan_finish_date` ← только `/form`, никем не читалось). Сроки
  теперь пишутся в `current_schedule` (тем же путём, что и `/gantt`) — и
  с инлайн-полей таблицы `/shift` (автосохранение по ячейке), и из
  модалки. `/status`/`/dashboard`/`/today` переведены на
  `coalesce(current_schedule, baseline_schedule)` — новый срок виден
  и там. Кнопка «Обновить график СМР» — ручной пересчёт отображения,
  не единственный путь к корректности. Разбор — `KNOWN_ISSUES.md` §51.
- Работы на 100% — в `<optgroup>` «Выполнены физически» внизу списка,
  не пропадают из выбора.
- «Подрядчик» в форме ввода факта переиспользует справочник
  `subcontractor` (та же логика, что уже была в
  `/api/gantt/subcontractor`, вынесена в общую функцию).
- `/status`: убран блок «Отклонение от директивного срока по неделям».
  `/today`: «Ограничения к снятию» перенесены на `/blockers` (снятие/
  правка срока сохранены), пункт меню «Почему отстаём» убран (страница
  и «Ввод → Стоп-фактор» работают).
- **Найдено и исправлено по пути, критично**: триггер
  `daily_progress_audit_trap_trg` ронял в проде ЛЮБОЕ повторное
  исправление уже введённого факта (не только для этой задачи —
  затрагивало всю группу СМР). Причина — отсутствующая таблица
  `daily_progress_audit_trap`, заведена миграцией 040. Разбор —
  `KNOWN_ISSUES.md` §53.
- Найден и задокументирован (не решён целиком) более широкий разрыв:
  `current_schedule`/`work.plan_finish_date` и ряд центральных таблиц
  ИД/РСК (`id_folder`, `change`, `prescription`, `id_package` и другие)
  не заведены ни одной миграцией — `KNOWN_ISSUES.md` §52.
- См. `docs/RUN_20260917_smr_shift_and_schedule.md`.

### Задача 2 — РСК (ТЗ Якименко А.И., 16.09.2026)
- **«Обзор РСК» — шесть тайлов**, идентичных на `/dashboard` и
  `/rsk/dashboard` (единый источник `compute_rsk_dashboard_stats()`):
  `Активных замечаний` · `Готовы к снятию` · `Предъявить ИД` ·
  `Корректировка ПД/РД` · `Отклонено РСК` · `Устранено` (новый).
- **«Устранено» — слой 2** (`rsk_processing.resolved`/`resolved_date`,
  чекбокс + дата в форме `/rsk/processing`). НЕ трогает
  `rsk_violation.is_active`/`closed_in_act_id` — те остаются только за
  импортом акта (правило двух слоёв не нарушено). Нарушение может быть
  «Устранено», но ещё присутствовать в последнем акте — не ошибка,
  показывается как есть.
- **«ИКС» скрыта** (`rsk_responsible.active=false`), не удалена — 11
  записей `rsk_processing_responsible` уже ссылались на неё
  (диагностика ДО правки). Скрыта из формы отработки и фильтра реестра;
  там, где уже реально записана, имя остаётся видимым (см.
  `KNOWN_ISSUES.md` §47 — прямое противоречие в самом задании,
  разрешено консервативно). «Стройка ТМ-35» → «строй-площадка» одной
  правкой в справочнике.
- **«Физика»** — аббревиатуры раскрыты (`не треб.`→`не требуется` и
  т.д.), `факт (нужна корректировка РД)` убран из выбора (легаси-строки
  отображаются). `needs_rd` переведён с `track_phys='fact'` на трек
  «Проект» (`track_design='not_done'`, легаси `fact` считается тоже) —
  тайл вырос с 32 до 35 (`KNOWN_ISSUES.md` §48).
- **«Условие снятия»** — слово «неделя» убрано из всех пяти подписей
  `rsk_close_condition`.
- **Реестр**: убраны дропдауны «Акт»/«Повторность»; «Состояние»
  получил исправленную `selected`-логику (была визуально сломана);
  разведены колонки «Снято» (структурно, было по ошибке подписано
  «Устранено») и «Устранено» (слой 2, новая).
- **«Загрузка акта» убрана из меню РСК и с `/rsk/dashboard`** — маршрут
  `/rsk/import` остаётся рабочим по прямой ссылке:
  **`https://tm.asd-kontur.ru/rsk/import`** (не терять — не в меню,
  открывать по этому URL или из закладки). Проверено 17.09.2026:
  загрузка существующего акта дала корректный предпросмотр/диф.
- См. `docs/RUN_20260917_rsk_section.md`.

### Задача 1 — ИД (ТЗ Якименко А.И., 16.09.2026)
- «Стоимость» унифицирована в одно слово «Стоимость, ₽» везде
  (экраны/CSV), значение — `coalesce(amount_smeta_rub, amount_rub)`
  через `id_folder_cost()`. Пять денежных тайлов, идентичных на
  `/dashboard` и `/id-folders` (новый «Подписано ранее, ₽», граница
  10.03.2026 в `app_setting`). «Папки» пересобраны: новый порядок
  секций, 6 колонок таблицы, 6 значений фильтра «Статус».
- См. `docs/RUN_20260917_id_stoimost_unification.md`.
- «Папки» и «Реестр папок» слиты в одну страницу `/id-folders` (задача
  до этой). См. `docs/RUN_20260917_id_folders_merge.md`.

## Следующее
1. Первичные документы РСК/ИЗМ (акт №4183-156, построчные реестры) — на
   Google Drive физически нет, запрошены у ССР/РСК напрямую.
2. Пункты «Требует решения координатора» ниже — ждут решения, не
   блокируют очередь.

## Требует решения координатора
- ЗАКРЫТО 24.09.2026 (`KNOWN_ISSUES.md` §66, follow-up): шесть плиток
  «Обзора РСК» и разрез «по ответственным» переведены на
  `rsk_violation_is_removed()`; `/rsk/processing` и `id_row_rsk_link`
  ЯВНО оставлены на `is_active` решением координатора (легитимные
  сценарии — исправление ошибочной отметки «Устранено», привязка
  снятого нарушения к разделу ИД), добавлен только статус-бейдж.
- Открыт без изменений (`KNOWN_ISSUES.md` §66, п.3): срок второго акта
  РСК — не задача Claude Code, ждёт РСК/ССР.
- Новейшее, 24.09.2026 (`KNOWN_ISSUES.md` §65, ТЗ №11): `/id-folders/-
  bulk-entry` по-прежнему пишет `signed_date`/`ks2_date` раздельно —
  может развести даты, которые карточка папки теперь держит вместе.
  Привести массовый ввод к тому же «одна дата на обе колонки»?
- Новейшее, 24.09.2026 (`KNOWN_ISSUES.md` §65): стадия трубы «Следующая
  КС-2» структурно недостижима для новых папок после слияния Подписания
  и КС-2 (на 24.09.2026 и без этого была пустой — эффект нулевой сегодня).
- Новейшее, 24.09.2026 (`KNOWN_ISSUES.md` §65): «Последние переходы» на
  `/id-folders` покажет две строки с одинаковой датой на каждое будущее
  слитое сохранение — схлопнуть в одну, не запрошено этим заданием.
- Новейшее, 24.09.2026 (`KNOWN_ISSUES.md` §65): `amount_smeta_rub`
  остался с одним путём заполнения (bulk-entry) вместо двух — судьба
  поля не решалась.
- ЗАКРЫТО 24.09.2026: VAT-вопрос про `id_folder.amount_rub` и другие
  ручные денежные поля — координатор подтвердил, что у них нет
  фиксированного налогового базиса, подпись «с НДС» отражает решение
  заказчика, не требует правки данных. `KNOWN_ISSUES.md` §63.
- ЗАКРЫТО 24.09.2026: дефолт `/gantt` — не «сегодня−7» (это было ошибкой
  прошлого захода, ошибочно названной отменой решения координатора),
  а весь директивный период целиком, без конфликта с решением от
  01.09.2026. `docs/RUN_20260924_tz10_followup.md`.
- ПОДТВЕРЖДЕНО 24.09.2026, без изменений: РСК «Снятые» технически
  корректен (пункт не задизаблен, пустой результат — обычное пустое
  состояние) — будет честно пустым, пока не появится второй акт РСК
  для сравнения с первым (№4183-159, 11.08.2026). Когда ожидается
  следующий акт — по-прежнему открытый вопрос координатору.
- Новейшее, 24.09.2026 (`KNOWN_ISSUES.md` §63): горизонтальный скролл на
  390px (телефон) — сайтовая характеристика на всех проверенных
  страницах, включая нетронутые этой задачей, ранее не измерялась
  (`ui_audit.py` меряет только 1366/1920/2560) — считать ли телефонную
  ширину частью критерия приёмки.
- Новейшее, 24.09.2026 (`KNOWN_ISSUES.md` §63): подпись «с НДС» не
  добавлена к сумме внутри трубы папок (`_id_folder_funnel.html`) —
  отдельного места под подпись там нет, нужна ли она и в каком виде.
- Новейшее, 21.09.2026 (`KNOWN_ISSUES.md` §62): задание про статус-
  фильтр `/id-progress` описывало справочник `id_form_status` таким,
  каким он был ДО 03.09.2026 (лишний статус только на «Труба Сварка») —
  на деле вопрос уже закрыт координатором (§14) 18 дней назад, список
  сейчас идентичен на всех 17 вкладках. Done-критерий «показать дропдаун
  отличается по вкладке» не выполнен буквально — восстанавливать статус
  ради демонстрации не стал (отменило бы принятое решение). Нужно
  явное «закрыть вопрос, премиса устарела» или новое указание.
- Новейшее, 21.09.2026 (`KNOWN_ISSUES.md` §61): легенда «Поток по
  вкладкам» и легенда матрицы «Раздел × Этап» на `/id-progress` — одни
  цвета, разные множества статусов (совпадает только зелёный).
  Рекомендация — выравнивать стрим к матрице (не наоборот, см. run log
  §3), но какую схему оставить итоговой — решение координатора, не
  менялось в рамках этого задания.
- Новейшее, 21.09.2026: задание называло `/id-progress` единственным
  исключением из «таблица обязана помещаться в рамку»; CLAUDE.md §7
  называет три (`/id-progress`, `/gantt`, `/shift`) — оставлены все три
  (CLAUDE.md авторитетен), не сузил список по своей инициативе. Если
  задание имело в виду сузить его — отдельное решение, ломающее две
  намеренно широкие таблицы.
- Новейшее, 21.09.2026: `/id-grafik/category-mapping` — та же форма
  («колонка на категорию»), что у трёх исключений выше, но не в списке
  CLAUDE.md и не выделена своим классом контейнера — формализовать как
  четвёртое исключение или перепроектировать, решение координатора
  (`KNOWN_ISSUES.md` §59).
- Новейшее, 19.09.2026: строки 25-26 ТЗ Якименко (формулы «Текущая
  КС-2»/«Следующая КС-2» в трубе «Выполнение») — по-прежнему не решены
  (`KNOWN_ISSUES.md` §42), ТЗ §5 прямо запрещает трогать формулы трубы
  без отдельного решения — не тронуто и в этом заходе.
- Новейшее, 19.09.2026: строка 23 ТЗ Якименко — сам текст ТЗ
  противоречит себе (требует «первые три сектора Папок идентичны
  Обзору ИД», но явно перечисляет РАЗНЫЕ списки тайлов «Разделов» для
  каждого — строки 16 и 22); решено в пользу буквальных списков, не
  идентичности — координатор может выбрать другую сторону.
- Новейшее, 18.09.2026 (инвентаризация): «Всего по контракту, ₽»
  (`/dashboard`, `/id-folders`) — хардкод-константа в Python
  (`ID_FOLDER_CONTRACT_TOTAL`), не таблица `contract` (существует в БД,
  не читается приложением). Числа сейчас совпадают, но не связаны —
  перевести тайл на таблицу или оставить как есть, решение координатора.
- Новейшее, 18.09.2026: расчёты трудоёмкости (`/calculator`, `/status`,
  `/dashboard`, `/today`, `/critical`) считаются живым SQL по срезу
  `daily_progress` (`source='excel_import'`), не пополнявшемуся с
  29.08.2026 — новая работа/пересмотр объёма после этой даты не попадёт
  в расчёт без повторного прогона импорта или нового способа ввода
  трудоёмкости, которого сейчас нет.
- Новейшее, 18.09.2026: `baseline_schedule` — фактически **149** строк,
  не 146, как давно считалось (в т.ч. в этом файле и в тексте задания) —
  расхождение не расследовано.
- Новое, 18.09.2026: часть C задания «довести ТЗ до конца» просила
  построить построчную таблицу ТЗ 16.09.2026 по цитатам в трёх run log
  17.09.2026 — цитат там нет (проверено, все три прочитаны целиком),
  только пересказ по разделам. Таблица в `docs/RUN_20260918_shift_-
  form_and_tz_audit.md` построена по пересказу; если у координатора
  сохранился исходный файл/сообщение с ТЗ — стоит сверить таблицу с ним
  напрямую, пересказ не гарантирует пословного соответствия.
- Новое, 18.09.2026: 18 строк `daily_progress` с утраченным исправлением
  (не первым введённым значением) — полный список
  `docs/DAILY_PROGRESS_LOST_EDITS_2026-09-18.md`. Задание прямо
  запретило исправлять их в рамках расследования — решение, что делать
  с каждой строкой, за координатором.
- KNOWN_ISSUES §52 (17.09.2026): полная реконструкция схемы БД в
  миграциях — `current_schedule`/`work.plan_finish_date` заведены
  задним числом этой задачей, но `id_folder`/`change`/`prescription`/
  `id_package` и другие центральные таблицы ИД/РСК всё ещё не заведены
  ни одной миграцией. Отдельная задача, не начата.
- KNOWN_ISSUES §54 (новое): страница `/losses` («Надёжность
  планирования по неделям») не связана ни с одним пунктом меню и
  дублирует заголовок убранного этой задачей пункта «Почему отстаём».
- KNOWN_ISSUES §56 (новое): `tools/functional_test.py` содержит
  существенную, не связанную с этой задачей устарелость (белый список
  маршрутов и три сценария ссылаются на давно не существующий вид
  «простого ввода факта» на главной) — не в обязательном чек-листе
  деплоя, не исправлялось.
- KNOWN_ISSUES §47 (задача 2): «ИКС» технически видна на экране там,
  где уже реально записана (11 карточек РСК) — прямое противоречие
  между «нигде не видна» и «ни одна запись не теряет ответственного» в
  самом задании; разрешено в пользу сохранности данных.
- KNOWN_ISSUES §48 (задача 2): тайл «Корректировка ПД/РД» вырос с 32 до
  35 после перевода источника с `track_phys='fact'` на трек «Проект».
- KNOWN_ISSUES §50 (задача 2): ссылка «Загрузить акт →» убрана и с
  `/rsk/dashboard`, не только из меню — расширение буквальной
  формулировки задания.
- KNOWN_ISSUES §45 (задача 1): тайлы «Подписано ранее, ₽»/«Подписано по
  КС-2, ₽» показывают честный 0,00 ₽ против упомянутых в этом файле
  ≈82 млн ₽ на 08.04.2026 — решение, заносить ли историю задним числом.
- KNOWN_ISSUES §44: шрифт < 14px сайтово в `.badge`/`code`/
  `.nav-label`/`.hotkey-hint`/`.kpi-label`/`.kpi-sub` — 2667 находок
  `ui_audit.py` на 18.09.2026 (снизилось с 2676 после удаления модалки
  «Ввод факта», см. выше — не решение сайтового долга, только удаление
  одного источника находок; долг остаётся открытым). Поднять до 14px
  сайтово или завести отдельный пол для служебных мелких надписей —
  решение не моё.
- `/id-rsk-link`, `id_form_row.id=968`: `section_label='Опв1- УТ1'` —
  висящий дефис и строчная «п» в данных, не нормализовал молча.
- KNOWN_ISSUES §43: `reload_id_form_v2.py`/`reload_id_rows_v3.py`
  перезапишут ОПн197/ОПн198 при повторном запуске.
- KNOWN_ISSUES §42: резолюция внесена задачей 1 (единый остаток ИД) —
  «Текущая КС-2» в трубе по-прежнему кумулятивно, не по периоду,
  осталось открытым.
- `/id-grafik` существует отдельно от `/id-progress`, недостижима из
  меню ИД (пауза координатора 11.09.2026).
- KNOWN_ISSUES §41: 24 записи `id_form_entry` без вида работ (было 23 на
  14-15.09.2026, +1 естественным дрейфом — пересчитано 18.09.2026 в
  ходе перестройки `/quality`, см. новую проверку там же) прячут
  разделы у «Электрики» на `/id-progress`.
- KNOWN_ISSUES §40: подпись недели на `/id-progress` — диапазон вместо
  общего правила «дата понедельника».
- ЗАКРЫТО 25.09.2026: `docs/PROD_SYNC_2026-09-15.md` п.5 утверждал, что
  `pdfplumber`/`openpyxl` из requirements.txt не установлены в живом
  образе — устарело, проверено заново (`pip list` в контейнере,
  `docs/RSK_act_parser_explained.md`, §«Находки»): `pdfplumber 0.11.10`
  реально установлен и используется (`rsk_parser.py`). `openpyxl` не
  перепроверялся отдельно в этом заходе.

## Задеплоено
`ee728917bc1b2d824400c611436fda8f884a280b` в проде на 25.09.2026
(«Обзор РСК» — новый набор плиток, ИКС/Лаборатория убраны из отделов,
`KNOWN_ISSUES.md` §69), сверка контейнер/git — сверено после нескольких
`curl`-обращений для сброса буфера строки лога (`docker logs tm_backend
| grep '[deploy]'` совпадает с `git rev-parse HEAD`).
`check_consistency.py` — **95/95** без расхождений (было 89, +6: все
шесть плиток по HTTP тем же href, что на экране; разбиение по отделам).
`ui_audit.py --no-shots` на официально задеплоенном коммите: 44
маршрута, 3285 находок, **2640 провальных** — без изменений против
предыдущего захода, новых категорий нет. Живая проверка: тайлы
152/16/15/94/33/10, `/rsk?state=active&status=needs_rd` → «Найдено: 33»
(совпадает с тайлом «Корректировка ПД/РД» после исправления).

Хронология предыдущего захода (25.09.2026, РСК: переключатель/один
отдел/«Загрузка акта», для истории):
`8e3ce53b34bdfb5ca00d8efe3f7abd8809c32d84`, `check_consistency.py`
89/89 (было 85, +4), `ui_audit.py`: 3285 находок, 2640 провальных, без
изменений против позавчерашнего.
Живая проверка на официально задеплоенном коммите: фильтр `/rsk?-
responsible=1` — 100 (сверено прямым SQL), `/rsk/dashboard` тайлы
136/2/52/34/10/16.

Хронология предыдущего захода (25.09.2026, «Обзор ИД» — две плитки в
зелёный, для истории): `bb7f42fde2fc3fa89dedad7114b63d2882755fde`,
`check_consistency.py` 88/88, `ui_audit.py`: 44 маршрута, 3285 находок,
2640 провальных (small_font, +3 против позавчерашнего — на `/id-entry`,
не тронутой этой правкой странице, реальная пользовательская
активность между прогонами, не регрессия).

Хронология захода 24.09.2026 (follow-up к РСК «Снятые», для истории):
`06d17883e1047281f49a4ee98b31e2631bfc6969`,
`check_consistency.py` 88/88 (было 85, +3 новые проверки — см.
`KNOWN_ISSUES.md` §66 follow-up). `ui_audit.py --no-shots`: 44
маршрута, 3285 находок, 2637 провальных — все `small_font` (сайтовый
долг §44), без новых провальных категорий (новая колонка «Состояние»
на `/id-rsk-link` не входит в штатный список маршрутов `ui_audit.py`,
проверена отдельно скриншотом). Прямая живая проверка на официально
задеплоенном коммите: `/rsk/dashboard` тайлы 136/2/45/35/4/16,
`/rsk?state=closed` → «Найдено: 16», совпадает с тестовой копией до
формального деплоя.

Хронология захода 24.09.2026 (РСК «Снятые», для истории):
`c8629ca7abbcec8f59513af1c848232cd8d0244b`, `check_consistency.py`
85/85, `ui_audit.py`: 44 маршрута, 3285 находок, 2637 провальных
(`column_over_allocated` 507→498, `container_width`
126, `table_overflow` 18, `short_row` 6 — все информационные, без
изменений по существу). Прямая живая проверка `/rsk?state=closed` →
«Найдено: 16», `/rsk?state=active` → «Найдено: 136» на официально
задеплоенном коммите.

Хронология предыдущего захода (24.09.2026, ТЗ №11, для истории):
`c3745e796dbc0f3af05926423a2b08627d40f70c`, `check_consistency.py`
82/82, `ui_audit.py`: 44 маршрута, 3282 находки, 2637 провальных.
Отдельно — телефон (390px, вне штатного
покрытия `ui_audit.py`): `/dashboard` `scrollWidth` упал с 1174 до 452
(побочный эффект удаления широкого `.bar-row` с /dashboard, не
регрессия), `/id-progress` (1197, компонент там остался) и карточки
папок (452) — без изменений.

Хронология предыдущего захода (24.09.2026, поправка к ТЗ №10, для
истории): 44 маршрута, 3294 находки, 2637 провальных, `column_over_-
allocated` 489 → 507.

Хронология захода 23.09.2026 (для истории): 44 маршрута,
3276 находок, 2637 провальных (снижение с 2700 — удаление трубы с
`/dashboard`), `column_over_allocated` 474 → 489.

Хронология провальных категорий по прошлым заходам:
- `small_font` (сайтовый долг §44): 2676 → 2667 → 2646 → 2697 → 2700 →
  2700 → 2637 → 2637 (снижение 24.09.2026 — см. выше, не регрессия;
  РСК «Снятые» без изменений — тот же 2637).
- `column_over_allocated` (24.09.2026, РСК «Снятые»): 507 → 498
  (сайтовый долг, побочный эффект другого распределения бейджей
  «Снято»/«В работе» по 16 строкам, не дефект).
- `kpi_row_wrap` (19.09.2026): 6 → 0.
- `table_overflow` (21.09.2026, новая проверка): 9 → 0 провальных
  (список исключений — `/gantt`/`/shift`/`/id-progress` по CLAUDE.md §7
  плюс `/id-grafik/category-mapping`, информационно, не провально —
  требует решения координатора).
- `column_over_allocated` (21.09.2026, новая, информационная): 381 → 474
  (ожидаемый побочный эффект меньшего паддинга, не дефект).
- `label_scope` (21.09.2026, новая проверка): 2 → 0 хита (`/rsk/-
  processing` — требует query-параметр, проверен отдельным точечным
  заходом, не сплошным обходом; `/rsk/import` — поймано сплошным
  обходом сайта).
- `horizontal_scroll`/`off_viewport`/`clipped_cell` — 0 везде на каждом
  заходе. `container_width`/`short_row` — без изменений.

Откат фабрикованной записи (работа `TM35-MAIN-043`, 19.09.2026) —
прямые SQL-операции на боевой БД, не файл кода, задеплоенного кода не
касался.
