# Текущее состояние работ

Обновлено: 17.09.2026

## Сейчас в работе
Ничего не в работе — задача 1 из 3 (ИД, ТЗ Якименко А.И. от 16.09.2026)
закрыта и задеплоена, см. ниже. Задачи 2 (РСК) и 3 (СМР) придут
отдельными заданиями.

## Последнее сделанное
- **«Стоимость» унифицирована**: «Оценка ПТО», «Сметная стоимость» и
  «Стоимость» — теперь одно слово «Стоимость, ₽» везде на экранах и в
  CSV. В БД по-прежнему два столбца (`amount_rub`/`amount_smeta_rub`,
  не слиты), значение — `coalesce(amount_smeta_rub, amount_rub)` через
  единственную функцию `id_folder_cost()`. Диагностика ДО правки: 0
  папок с обеими колонками заполненными, 0 подписанных вообще (см.
  `KNOWN_ISSUES.md` §45 — честный ноль на новых тайлах против
  упомянутых в этом файле ≈82 млн ₽, решение координатора).
- **«Папки» пересобраны**: порядок секций Деньги → Воронка → Разделы →
  Список папок; таблица — 6 колонок (`Номер папки` · `Дата создания` ·
  `Разделов` · `Стоимость, ₽` · `Подписант реестра` · `Статус`); два
  дропдауна (`Статус` — 6 значений, `Сортировка по номеру`). Старая
  строка плиток-итогов над таблицей убрана целиком.
- **Пять денежных тайлов**, идентичных на `/dashboard` и `/id-folders`:
  `Всего по контракту, ₽` · `Подписано ранее, ₽` (новый, граница даты
  10.03.2026 в `app_setting['id_signed_before_boundary_date']`,
  `migrations/037`) · `Подписано по КС-2, ₽` · `Невыбираемый остаток, ₽`
  · `Остаток по контракту, ₽`. Формула остатка одна на обе страницы —
  закрывает `KNOWN_ISSUES.md` §42 («два разных остатка»).
- **Тайлы «Разделы»** переименованы (числа не менялись): «Папки» — 5
  тайлов (с `Активных ИЗМ (ДПР)`), «Обзор ИД» — те же первые 4.
- Найден и исправлен по пути: `status=signed` (легаси-ссылка бывшего
  `/id-folders/registry`) не фильтровал список — теперь легаси-алиасы
  канонических стадий (`ID_FOLDER_STAGES`) работают через
  `id_folder_stage()`, редирект `?status=signed&sort=asc` приходит
  отфильтрованным.
- См. `docs/RUN_20260917_id_stoimost_unification.md`.
- «Папки» и «Реестр папок» слиты в одну страницу `/id-folders`
  (задача до этой). См. `docs/RUN_20260917_id_folders_merge.md`.
- `hotkeys.js`: подсказка «Ctrl+Enter» — по `allSaveForms().length===1`,
  не по классу кнопки. См. `docs/RUN_20260916_hotkey_hint.md`.
- Инвертирован умолчательный перенос текста в таблицах, пройдены все 35
  шаблонов, `tools/ui_audit.py` — реальный гейт в чек-листе деплоя. См.
  `docs/RUN_20260916_wrap_default.md`.

## Следующее
1. Задачи 2 (РСК) и 3 (СМР) — ждут отдельных заданий координатора.
2. Первичные документы РСК/ИЗМ (акт №4183-156, построчные реестры) — на
   Google Drive физически нет, запрошены у ССР/РСК напрямую.
3. КЗ-40/КЗ-41/КЗ-43/КЗ-44/КЗ-46 из `KNOWN_ISSUES.md` (см. ниже) — ждут
   решения координатора, не блокируют очередь.

## Требует решения координатора
- KNOWN_ISSUES §45 (новое, 17.09.2026): тайлы «Подписано ранее, ₽» и
  «Подписано по КС-2, ₽» показывают честный 0,00 ₽ — в БД `id_folder`
  сейчас 0 папок с `signed_date` вообще. Этот файл выше (раздел 2)
  упоминает ≈82 млн ₽, закрытых по КС-2 на 08.04.2026, — не проверено
  независимо и не занесено в `id_folder` реальными датами. Заносить ли
  историю задним числом (миграцией с настоящими `signed_date`/`ks2_date`)
  — решение не моё.
- KNOWN_ISSUES §46 (новое, 17.09.2026): дропдаун «Статус» на
  `/id-folders` по заданию — ровно 6 значений, без «Все». При заходе
  без `status=` список показывает все папки, а select визуально
  подсвечивает первый пункт («Сформировано») — известное, не
  исправленное без решения координатора расхождение.
- KNOWN_ISSUES §44: шрифт < 14px сайтово в `.badge`/`code`/
  `.nav-label`/`.hotkey-hint`/`.kpi-label`/`.kpi-sub` — 2598 находок
  `ui_audit.py` на 17.09.2026 (второй раз подряд то же число — дрейфа
  нет). Поднять до 14px сайтово или завести отдельный пол для служебных
  мелких надписей — решение не моё.
- `/id-rsk-link`, `id_form_row.id=968`: `section_label='Опв1- УТ1'` —
  висящий дефис и строчная «п» в данных, не нормализовал молча.
- KNOWN_ISSUES §43: `reload_id_form_v2.py`/`reload_id_rows_v3.py`
  перезапишут ОПн197/ОПн198 при повторном запуске.
- KNOWN_ISSUES §42: резолюция внесена этой задачей (единый остаток) —
  «Текущая КС-2» в трубе по-прежнему кумулятивно, не по периоду,
  осталось открытым (§5 задания запрещал трогать формулы трубы).
- `/id-grafik` существует отдельно от `/id-progress`, недостижима из
  меню ИД (пауза координатора 11.09.2026).
- KNOWN_ISSUES §41: 23 записи `id_form_entry` без вида работ прячут
  разделы у «Электрики» на `/id-progress`.
- KNOWN_ISSUES §40: подпись недели на `/id-progress` — диапазон вместо
  общего правила «дата понедельника».
- `docs/PROD_SYNC_2026-09-15.md` п.5: `pdfplumber`/`openpyxl` в
  requirements.txt, не установлены в живом образе.

## Задеплоено
`eedd0074d00bed2ffde6be9ccb78a162133a68a5` в проде на 17.09.2026, сверка
контейнер/git — момент коммита (`docker logs tm_backend | grep
'[deploy]'` совпадает с `git rev-parse HEAD`). `check_consistency.py`
73/73 без расхождений (было 68 — 5 новых проверок этой задачей).
`ui_audit.py --no-shots`: horizontal_scroll/off_viewport/clipped_cell —
0 везде; small_font — 2598, тот же счёт, что и до этой задачи (в
пределах дрейфа §44, не регрессия).
