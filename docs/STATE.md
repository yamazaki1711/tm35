# Текущее состояние работ

Обновлено: 16.09.2026

## Сейчас в работе
Ничего не в работе — последняя задача (ОПн197/ОПн198) закрыта и
проверена, см. ниже.

## Последнее сделанное
- Добавлены два пропущенных в исходном xlsx раздела вкладки «ОПН (рсм)»
  — ОПн197/ОПн198 (`migrations/036_id_opn_rsm_197_198.sql`, идемпотентна,
  бэкап данных в `import/opn_rsm_197_198_20260916/`). Все счётчики
  разделов сдвинулись ровно на +2 там, где положено, 0 — везде
  остальном (проверено до/после реальными вызовами). `check_consistency.py`
  68/68. Риск повторного импорта (`reload_id_form_v2.py`,
  `reload_id_rows_v3.py` перезаписали бы новые разделы) — в
  `KNOWN_ISSUES.md` §43, скрипты не трогал. См.
  `docs/RUN_20260916_opn_197_198.md`.
- «Обзор ИД» на `/dashboard` пересобран по ТЗ Якименко А.И. (ПТО) от
  15.09.2026 — деньги (4 тайла), труба «Выполнение» (5 новых стадий),
  разделы (4 тайла), навигация (4 карточки + тайл ИЗМ). `/id-folders` →
  «Папки» везде, где называла страницу. См.
  `docs/RUN_20260915_id_overview.md`. Деплой: `9160383` в проде.
- `77ff292`/`654d6fb` — деплой-дисциплина: правило "git → контейнер",
  печать запущенного коммита в лог, репетиция процедуры.
- `1bc34ed` — синхронизация с продакшеном 8 файлов, разошедшихся за
  прежние сессии (`docs/PROD_SYNC_2026-09-15.md`).

## Следующее
1. Первичные документы РСК/ИЗМ (акт №4183-156, построчные реестры) — на
   Google Drive физически нет, запрошены у ССР/РСК напрямую; до получения
   разделы «Предписания»/«Изменения» на `/closure` остаются заглушкой.
2. КЗ-40/КЗ-41/КЗ-42/КЗ-43 из `KNOWN_ISSUES.md` (см. ниже) — ждут решения
   координатора, не блокируют очередь.

## Требует решения координатора
- KNOWN_ISSUES §43 (новое, 16.09.2026): `reload_id_form_v2.py` (без
  всякой защиты) и `reload_id_rows_v3.py` (защищён только пока
  id_form_entry/id_form_block не пусты) перезапишут ОПн197/ОПн198 при
  повторном запуске — их источники (замороженный JSON / CSV от
  27.08.2026) новых разделов не знают. Сами скрипты не менял.
- KNOWN_ISSUES §42: два законных, но разных «остатка» — «Остаток по
  контракту, ₽» (/dashboard, от КС-2) vs «Остаток в деньгах, ₽»
  (/id-folders, от подписания). Свести к одному — отдельное решение.
- KNOWN_ISSUES §42: «Текущая КС-2» в трубе — кумулятивно за всю историю,
  не акт отчётного периода (в БД нет поля периода).
- `/id-grafik` существует отдельно от `/id-progress`, недостижима из
  меню ИД (пауза координатора 11.09.2026).
- KNOWN_ISSUES §41: 23 записи `id_form_entry` без вида работ прячут разделы
  (у «Электрики» 4 из 6 разделов с данными не видны на `/id-progress`) —
  рекомендация дана, правило подсчёта не менялось до ответа.
- KNOWN_ISSUES §40: глобальный `th { font-size: .72rem }` (11.52px) ниже
  пола проекта 14px на заголовках всех таблиц сайта.
- KNOWN_ISSUES §40: подпись недели на `/id-progress` — диапазон вместо
  общего правила «дата понедельника» — сделано сознательно, расходится
  с остальным сайтом.
- `docs/PROD_SYNC_2026-09-15.md` п.5: `pdfplumber`/`openpyxl` есть в
  `requirements.txt` в git, но не установлены в живом образе.

## Задеплоено
`9160383` в проде (код приложения, 15.09.2026) — не менялся этой сессией,
т.к. задача 16.09.2026 трогала только данные БД (миграция 036), не код.
`check_consistency.py` 68/68 без расхождений, сверка контейнер/git —
15.09.2026.
