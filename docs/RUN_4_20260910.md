# TM-35 — заход 4, 10.09.2026

Правила те же. Заход 3 принят координатором (43 проверки, коммит
`972ad80` про смешение документации с кодом — замечание учтено, без
доп. действий).

**Если сессия оборвалась** — читать этот файл, искать первую задачу не
в статусе ГОТОВО/ОТКАЗ, продолжать с неё.

## Состояние очереди

| # | Задача | Статус | Коммит | Начато | Завершено |
|---|--------|--------|--------|--------|-----------|
| 2 | Доказать, что "Замеч. РСК" реально попадает в файл | ГОТОВО (без кода) | — | 19:00 | 19:15 |
| 1 | Экран решения category→tabs для 58 несопоставленных групп | В РАБОТЕ | — | 19:15 | — |
| 3 | Дозагрузка очереди (проверки, аудит, тесты, документация) | ОЖИДАЕТ | — | — | — |

Порядок: задача 2 первой (короче, проверяет уже сделанное), затем
задача 1 (главная задача захода), затем 3.

## Журнал хода

Заход начат, репозиторий синхронизирован с контейнером (diff пуст).

19:00-19:15 — задача 2. Прикрепил замечание №387 к разделу КД-1
(row_id=1004, группа source_row=65) через настоящий экран
/id-rsk-link (не напрямую в БД), сгенерировал /export/id-grafik.xlsx
через настоящий HTTP-запрос браузера, скачал файл и прочитал ячейки:

- C65 (Статус): "Подписано" — как и раньше, не изменилось этой
  проверкой.
- P65 (Замеч. РСК): "387-На интервале тепловой сети ОПв 1.1-ОПн 52 в
  местах опирания…" — короткая форма, замечание реально в файле.

Полная сверка через openpyxl (шаблон vs файл, все ячейки листа
"График ИД"): 56 расхождений всего, из них 55 — уже известные патчи
статуса (колонка C), P65 — единственное расхождение вне колонки C, и
оно ожидаемое. Ничего постороннего не сдвинулось. Самопроверка
инварианта прошла (файл вообще отдался, 200, не ушёл в откат-на-
ошибку). Открепил замечание сразу после проверки через тот же экран
(кнопка "Открепить") — `id_row_rsk_link` снова пуста (0 строк).
Кода не менял — задача 2 была именно про проверку уже сделанного.

19:15 — начинаю задачу 1 (главная задача захода). Разбор перед кодом:
1a — матрица категория×вкладка со счётчиками совпадений по
префикс+номер; 1b — экран с чекбоксами на сохранение в новую таблицу
id_report_category_tab; 1c — пересчёт сопоставления только для
сохранённой категории, идемпотентно (повторный запуск не меняет уже
привязанное). Нужно изучить существующий импорт-скрипт
tools/import_grafik_id_groups.py и его логику
резолва по префиксу+диапазону, чтобы переиспользовать её, а не писать
вторую параллельную реализацию того же алгоритма сопоставления.
