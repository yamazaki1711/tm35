# Ночной прогон ТМ-35 — 09-10.09.2026

Автономный прогон по очереди задач из промпта координатора
(09.09.2026, 16:19 по объекту). Правила: см. сам промпт, §0. Кратко —
никогда не заканчивать ход вопросом, развилки без решения в таблице —
консервативное умолчание + запись сюда + дальше, одна задача = один
коммит, самопроверка → деплой, провал самопроверки за 2 попытки →
revert + ОТКАЗ + следующая задача.

**Замечание по режиму (зафиксировано сразу, не блокирует очередь):**
это интерактивная CLI-сессия, не отдельный демон — буквальные "20 часов
без участия координатора" гарантировать механически не могу (нет
активного /loop или scheduler на эту задачу). Работаю максимально
непрерывно в рамках этой сессии, состояние в этом файле — обязательно
актуальное на любой момент, чтобы прогон можно было продолжить с той
же точки в новой сессии командой "продолжай ночной прогон".

**Файлы ТЗ, на которые ссылается промпт, не найдены на диске:**
`cc_prompt_obzor_papki.md` (задача 3) и `cc_prompt_obzor_id_grafik.md`
(задача 4) — искал по всему `/home/oleg` и `/`, нет нигде, "передан
отдельно" не дошло. Задачи 3 и 4 делаются строго по инлайн-краткому
изложению и критериям "Готово, когда" из самого промпта — это менее
подробно, чем полное ТЗ могло бы быть. Записано в «Требует решения
координатора» тоже.

## Состояние очереди

| # | Задача | Статус | Коммит | Начато | Завершено |
|---|--------|--------|--------|--------|-----------|
| 1 | Легенда РСК-амбер + честный отказ выгрузки | ГОТОВО | ee5f322 | 16:19 | 16:55 |
| 2 | Статус «Подписано в карандаше» | ГОТОВО | be3b7ea | 16:56 | 17:35 |
| 3 | «Обзор ИД» вокруг папок | ГОТОВО | bf09dfc | 17:36 | 18:55 |
| 4 | Страница «График ИД» (блоки 1-2 из 4) | ГОТОВО-ЧАСТИЧНО | d893c21 | 18:56 | 10:24 |
| 5 | Разбор 116 несопоставленных строк (фон) | ГОТОВО | 460e62b | 17:40 | 17:43 |
| 6 | Сеть регрессионных проверок (фон) | ОЖИДАЕТ | — | — | — |
| 7 | Документация (добивка) | ОЖИДАЕТ | — | — | — |
| — | Зачистка пояснительных подписей под заголовками (правило координатора) | ГОТОВО | ff4094e | 10:24 | 10:35 |

## Журнал хода

16:19 — прогон начат. Пересобрал контекст: main на 76d86bb (часть 5
экспорта «График ИД»). Файлы ТЗ задач 3/4 не найдены — см. замечание
выше. Начинаю задачу 1.

16:55 — задача 1 готова. Проверил живой справочник `id_form_status`
напрямую (17 вкладок × 9 значений, полностью уникально): текста
"Передано на проверку" (из промпта) в справочнике нет вообще — амбер-
правило РСК-цикла реализовано на реальных 4 значениях ("Первичная
проверка РСК", "Устранение замечаний 1", "Повторная проверка РСК",
"Устранение замечаний 2"), не на выдуманном тексте. Проверено на живой
выгрузке — все 8 строк с "Первичная проверка РСК" (КД14, КД15, КД16,
КД16.1, КД18, КД19, КД20, КД22) стали янтарными (FFFFC000).
Честный отказ выгрузки — проверен по-настоящему: временно вставил
искусственный `raise` в `_xlsx_verify_only_patched_changed` прямо в
задеплоенный `/app/main.py`, перезапустил контейнер, убедился, что
`/export/id-grafik.xlsx` отдаёт 303 → `/id-grafik?err=...` → страница
рендерит `flash-err` с текстом «Выгрузка не сформирована...», а не
xlsx (content-type text/html, не файл). Вернул рабочий `main.py`,
перезапустил, обычная выгрузка снова 200 xlsx. Стопперы ("Нет
проектного решения", "Замечания к площадке") оставлены без заливки —
см. «Требует решения координатора».
Начинаю задачу 2.

17:35 — задача 2 готова. Миграция 031: "Подписано в карандаше" вставлен
на display_order=7 (сразу перед "Подписано") во всех 17 вкладках,
существующие 7-9 сдвинуты на +1 (проверено на tab_id=1: 10 значений,
порядок верный). Аудит потребителей "Подписано" в main.py: ВСЕ SQL-
счётчики (`compute_id_folder_stats`, `id_stats_row` на дашборде,
`ID_AVAILABLE_ROWS_SQL` для сборки папок) используют точное
`s.code='Подписано'`, новый статус (другой code) их не задевает
структурно, без правок кода. Ловушка нашлась и исправлена только в
экспорте «График ИД» — `status_fill_color`/`_status_tier`/
`_status_is_full` проверяли `.startswith("подписано")` ради "Подписано
80%", и "Подписано в карандаше" под это тоже подходило бы (уехало бы
Заказчику зелёным). Добавлено явное исключение до общего правила во
всех трёх функциях.
Проверено на живых данных, не только на бумаге: вставил тестовую
`id_form_entry` (раздел КД14, статус "Подписано в карандаше"), проверил
— (а) в подсчёте "s.code='Подписано'" раздел не участвует, (б) в
запросе доступности для сборки папки раздел не проходит, (в) в
`/export/id-grafik.xlsx` строка 134 (КД14) стала статусом "Подписано в
карандаше" с янтарной заливкой FFFFC000, не зелёной. Тестовую запись
удалил после проверки (id=1963, `responsible_name='ночной прогон
тест'` — не настоящие данные).
17:40 — запустил задачу 5 в фоне (setsid nohup, PID-lock
`/tmp/tm35_night_task5.pid` внутри контейнера), как и предписано —
пока считает, перехожу к задаче 3. Скрипт отработал почти сразу (это
разбор в памяти по ~1400 строкам БД, не долгий прогон) — 17:43 уже
готов результат: **было 52 группы хотя бы с одним разделом → стало
75 из 133** (доукомплектовано 23). Новое правило (сверх части 3):
подсказка «категория → вкладка» обобщена с одного проверенного случая
до общего — название вкладки, целиком входящее в текст категории,
берётся как подсказка, ЕСЛИ пересечение с реально коллидирующими по
префиксу вкладками даёт ровно одну (не гадание по названию категории
самому по себе — точное пересечение множеств). «Камеры и колодцы»
намеренно осталась неоднозначной там, где пересечение даёт больше
одной вкладки. Отчёт — `docs/match_groups_v3_report.md`. Оставшиеся 58
— та же структурная коллизия н/опн/опв из decisions_needed п.18, новых
идей для нее не появилось, решение по-прежнему за координатором.
Начинаю задачу 3.

18:55 — задача 3 готова (главная задача прогона). Миграция 032:
id_folder += check_start_date, signed_date, signed_by, ks2_date,
ks2_no, amount_smeta_rub. Стадия НЕ хранится отдельным полем —
`id_folder_stage()` в main.py, единственное место, вычисляет по
последней заполненной дате цепочки Сформирована → Передана в СДО →
Проверка → Подписана → КС-2 (та же дисциплина, что весь проект — не
дублировать вычислимое состояние в колонке, см. AUDIT_DATA_INTEGRITY).

**Старое и новое значение «подписано в деньгах» — как и требовалось,
рядом:**
- СТАРОЕ (по факту передачи в СДО, `amount_rub`): **3 285 192,73 ₽**
- НОВОЕ (по факту подписания, `amount_smeta_rub`): **0,00 ₽**

Значение упало до нуля, как и предсказывал критерий приёмки — это
честно: ни одна из живых папок пока не прошла стадию "Подписана" в
новой модели (только собраны/1 передана в СДО), amount_smeta_rub нигде
ещё не введён. Как только ПТО начнёт вести подписание через новые
формы, число появится само.

`compute_id_folder_stats()` переписана: `signed_folders_sum` теперь
= сумма `amount_smeta_rub` папок с `signed_date is not null` (не
`amount_rub` папок с `sdo_transfer_date` — старая формула). `amount_rub`
не удалён, подписан в интерфейсе как «Оценка ПТО, ₽» (не денежный
показатель, явное пояснение на карточке папки).

Дашборд `/id-folders` перестроен на 4 строки — воронка (5 стадий,
количество + сумма там, где известна), деньги (контракт/подписано/
ручной объём/остаток), разделы (всего/подписано/остаток/готово к
сборке/активных ИЗМ (ДПР)), последние переходы (таблица по всем
папкам). Новые формы на `/id-folders/{id}`: Проверка (дата), Подписание
(дата + подписант из RSK_SIGNERS — таблица решений), КС-2 (дата +
номер), Сметная стоимость (доступна только когда signed_date заполнена
— проверено и на сервере, не только скрыто в форме). `/id-folders/registry`
и `/export/id-folders.csv` — стадия через ту же функцию, не отдельный
булев признак; денежная плитка переименована `amount_transferred` →
`amount_signed` вместе со сменой смысла, чтобы название не разошлось с
расчётом.

Проверено: сумма по 5 стадиям воронки сходится с count(*) from
id_folder — было 9 папок на старте задачи, к моменту проверки
координатор (или кто-то ещё) **создал десятую папку ТМ-011 через
обычную форму сборки прямо во время прогона** — воронка корректно
показала 10 (9 формировано+1 передана), сумма стадий = count(*).
Обнаружение живой параллельной активности зафиксировано отдельно ниже.
Полный цикл стадий (Проверка→Подписана→КС-2, включая сметную
стоимость) проверен на изолированной тестовой папке (создана и удалена
мной, `id=12`, ФИО `НОЧНОЙ-ТЕСТ-2`, не настоящие данные) — money_remaining
корректно уменьшился ровно на введённую сметную стоимость, стадия
корректно определилась как "КС-2". Прямой HTTP POST по новым
эндпоинтам не тестировал (401 без сессии координатора) — логика форм
идентична уже проверенному /sdo, проверил на прямых изменениях в БД +
пересчёте `compute_id_folder_stats()`.
Скриншоты `/id-folders` и `/id-folders/{id}` сняты и просмотрены — нет
горизонтальной прокрутки, нет новых нарушений минимального шрифта
(11.52px — тот же досессионный `TH`, не новый).

**Обнаружено во время прогона:** на сервере кто-то ЖИВОЙ активно
работает параллельно (новая папка ТМ-011 создана через штатную форму,
новые записи `id_form_entry` появлялись и раньше в течение ночи) — не
мешал, тестовые данные создавал и убирал изолированно (папки id=10,12,
не пересекались с реальными id 1-11).
Начинаю задачу 4.

18:56-10:24 — задача 4, блоки 1-2 из 4 (плитки + «Поток по вкладкам»).
Гранулярность — связка раздел+вид работы, не раздел целиком (до ~24
видов работ на раздел). Добавлена вторая каноническая CTE
`LATEST_ID_FORM_ENTRY_BY_WORKTYPE_CTE` (схлопывает по row_id+work_type_id,
не только по row_id, как уже существующая `LATEST_ID_FORM_ENTRY_CTE`) —
обе легитимны, отвечают на разные вопросы, объединять их значило бы
повторить болезнь дублирующихся расчётов из аудита 08.09.2026. Блоки
3-4 (матрица-светофор «Раздел × Этап» с неделями 10.03.2026-01.06.2027
и drill-down в карточку раздела) не начаты по нехватке времени сессии —
недоделанные блоки в интерфейс не выведены (страница заканчивается
после потока, без заглушки-плейсхолдера — заглушки в интерфейсе тоже
запрещены, см. запись ниже). Пункт меню «Прогресс по видам работ»
добавлен сразу (страница функционально цельна на готовых блоках).
Проверено: `/id-progress` отдаёт 200, скриншот показал шесть плиток
(1475 связок, 360/1353 разделов, 1118 подписано, 0 в карандаше, 48 в
цикле РСК, 144 стоппера) и полосы по 11 из 17 вкладок с данными, нет
горизонтальной прокрутки. Коммит `d893c21`.

10:24-10:35 — координатор указал прямо (сообщение с расширенным
промптом): в интерфейсе ТМ-35 запрещены пояснительные подписи под
заголовками разделов — заголовок и сразу содержимое, объяснения
«почему так» это для сессии, не для экрана; конкретно назван подзаголовок
под «Выполнение — сборка папок ИД». Прошёл по всем шаблонам, изменённым
за эту сессию, и убрал: в `id_folders.html` — 2 подписи (в т.ч. названную);
в `id_folder_detail.html` — 3 подписи под заголовками (подпись с
фактическими датами папки под `<h1>` оставлена — это данные, не
пояснение); в `id_grafik.html` — 2 подписи под заголовком страницы,
а также обнаружил и убрал утечку внутреннего пути
`tools/import_grafik_id_unmatched_v2.csv` в подписи под плиткой (то же
нарушение: технический артефакт сессии на экране пользователя) и
техническую подпись «создаются все — из CSV, безусловно»; в
`id_progress.html` — подпись про гранулярность и прямую фразу «страница
в разработке» (недопустима на экране в принципе), а также заглушку
«Матрица «Раздел × Этап» — не сделано в этом прогоне» со ссылкой на этот
файл — теперь недоделанный блок 3-4 просто не отображается вообще, как
и требовала исходная инструкция задачи 4 «недоделанные блоки не
показывать». Все три шаблона перепроверены скриншотами после деплоя.
Коммит `ff4094e`. Правило занесено в `CLAUDE.md` проекта ТМ-35 отдельным
пунктом (см. ниже), чтобы не потерялось между сессиями.

## Требует решения координатора

- Файлы `cc_prompt_obzor_papki.md`/`cc_prompt_obzor_id_grafik.md` не
  дошли — задачи 3/4 сделаны по краткому изложению в самом промпте, не
  по полному ТЗ. Если полные версии существуют — пришлите, сверю и
  доделаю расхождения отдельным коммитом.
- Задача 1: стопперы («Нет проектного решения», «Замечания к
  площадке») оставлены без заливки, как и предписано; введение
  красного цвета в документ для Заказчика — решение ПТО, не моё.
- Задача 1: в промпте упомянут статус «Передано на проверку» для
  янтарной ветки — такого текста нет ни в одной из 17 вкладок живого
  справочника `id_form_status` (проверено прямым запросом). Амбер
  реализован на 4 реально существующих статусах РСК-цикла. Если
  «Передано на проверку» должен был появиться (например, задуман, но
  ещё не заведён в справочник) — скажите, добавлю правило заранее.
- Задача 3, без полного ТЗ (см. пункт выше) принял 3 самостоятельных
  толкования краткого изложения — если полное ТЗ говорит иначе,
  поправить легко, изолированные места:
  1. «Активных ИЗМ (ДПР)» — показал тот же счётчик «активных ИЗМ», что
     и на `/changes` (не завершены/не архивны), без отдельного фильтра
     по ДПР — по факту схемы `change` все действующие заявки и так от
     ДПР (см. decisions_needed п.27), отдельного признака «это не ДПР»
     в таблице нет.
  2. «Переходы» (строка 4) — простой список последних 10 событий по
     всем папкам (передана/начата проверка/подписана/КС-2), не более
     сложная визуализация — если задумывалась именно она, нужно ТЗ.
  3. 4-строчный дашборд построен на странице `/id-folders`
     («Выполнение — сборка папок ИД»), не на общем `/dashboard» — там
     это отдельный от СМР/ИД/РСК-обзора экран специально про папки,
     показалось логичнее не перегружать общий дашборд ещё одной
     воронкой. `home.html` получил только исправленную денежную цифру
     (структурно, через тот же `compute_id_folder_stats()`), без
     нового 4-строчного блока.
